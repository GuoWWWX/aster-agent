# 后端代码审查结论与分阶段优化计划

> 复核日期：2026-08-17
> 复核范围：`apps/desktop/src/main`、`packages/protocol`、后端相关测试与工程门禁
> 执行方式：一次只实施一个批次；自动测试通过并由用户手工确认后，才能开始下一批
> 相关规范：[后端编码规范](./13-后端编码规范.md)
> 业务依据：[业务上下文](./14-业务上下文.md)

## 1. 最终结论

当前后端已经形成可运行的本地 Agent 闭环，不属于需要推倒重写的项目。协议校验、IPC 错误边界、模型流式适配、SQLite 多写事务、文件搜索、命令超时与取消等局部实现是合理的，说明项目具备继续演进的基础。

初始审查发现 5 个会影响可靠性或运行事实的 P1 问题。第一批已修复设置测试漂移、IPC 注册/注销不对称和 Capability 错报；第二批已把会话与附件删除改为持久、可恢复的清理流程。当前仍有 1 个后端 P1 需要按批处理：

1. 最终 Assistant 消息、Run 终态和 Subagent 结果没有原子提交，崩溃恢复可能制造错误状态。

根级 Lint 仍被 Web 设置页和配置文件树中的 3 个 error、2 个 warning 阻断。Desktop 与 Protocol Lint 均通过，该前端问题不混入后端优化批次。

设计模式的总体判断是“方向正确、落地不完整”：Ports and Adapters、Strategy、Repository、Command/Event 已在部分边界出现，但 Tool 派发、模型协议、数据库职责和 Run 状态机仍有中央模块承担过多变化。应该按真实故障和变化轴逐步收口，不应立即建立插件平台，也不应为了匹配规划文档一次性创建所有 `packages/*`。

## 2. 复核方法与边界

本轮使用当前代码而非旧审查文本作为事实源，完成了以下检查：

- [x] 对照旧审查逐项定位当前文件和行为。
- [x] 统计 IPC Handler 注册与注销集合。
- [x] 检查会话/附件删除顺序与归档清理路径。
- [x] 检查最终消息、Run 终态和 Subagent 投递事务边界。
- [x] 检查工具、模型、数据库和配置存储的职责与重复实现。
- [x] 执行根级 `lint`、`typecheck`、`test`、`build`。

初始审查阶段没有修改后端代码、数据库结构或运行中的 Electron 进程，也没有调用外部模型。第一批实施记录见第 6.2 节。

严重程度定义：

| 等级 | 含义 |
| --- | --- |
| P1 | 会阻断质量门禁，或可能造成运行故障、状态错误、不可恢复数据残留 |
| P2 | 当前可运行，但继续扩展会显著放大修改面、行为漂移或回归风险 |
| P3 | 可维护性建议；没有证据表明必须立即修改 |

## 3. 当前质量门禁

| 命令 | 结果 | 当前证据 |
| --- | --- | --- |
| `pnpm lint` | 失败 | Desktop 与 Protocol 通过；Web 有 3 个 error、2 个 warning，位于设置工作区和配置文件树 |
| `pnpm typecheck` | 通过 | Protocol、Desktop、Web 全部通过 |
| `pnpm test` | 通过 | Protocol 32/32、Web 43/43、Desktop 173/173 |
| `pnpm build` | 通过 | Protocol、Desktop、Web 均构建成功；Web 主 JS chunk 有大于 500 kB 的非阻断警告 |

Web Lint 问题属于仓库基线阻断，但不纳入本后端重构的顺手修改范围；已在第一批验证记录中作为外部阻断保留。

## 4. 最终审查发现

### 4.1 P1：后端类型检查和测试基线未通过（第一批已修复）

`ApplicationSettings.general` 已包含 `defaultPermissionMode`、`defaultMessageDeliveryMode`、`sendShortcut` 和 `showContextUsage`，但测试仍构造和断言旧结构，导致 Desktop 类型检查失败和 2 个测试失败。

代码依据：

- `apps/desktop/src/main/settings/application-settings-store.test.ts:55`
- `apps/desktop/src/main/settings/application-settings-store.test.ts:73`
- `packages/protocol/src/application-settings.ts:145`

处理结果：测试夹具改为复用 `DEFAULT_APPLICATION_SETTINGS.general`，兼容迁移断言同步覆盖全部默认字段；未修改生产设置行为。Desktop Typecheck 和 166 项测试已通过。

### 4.2 P1：IPC Handler 注销集合缺少 14 个 Channel（第一批已修复）

初审时静态统计为注册 69 个、注销 55 个。遗漏如下：

```text
applicationSettingsGetConfiguration
applicationSettingsSaveConfiguration
contextCompressionGetConfiguration
contextCompressionSaveConfiguration
conversationReorder
conversationSetArchived
conversationSetPinned
conversationSetProject
modelTestConnection
projectCreateEntry
projectRemove
projectRename
projectReorder
projectSetPinned
```

窗口关闭时会执行 disposer；macOS 再次激活创建窗口后会重新注册 Handler。手写两份清单已经发生漂移，继续补一行 `removeHandler` 只能修复本次遗漏，不能防止再次发生。

代码依据：

- `apps/desktop/src/main/ipc/register-main-ipc.ts:228`
- `apps/desktop/src/main/ipc/register-main-ipc.ts:956`
- `apps/desktop/src/main/bootstrap/index.ts:264`

处理结果：新增窄 `createIpcHandlerRegistrar()`，实际注册集合同时用于完整性断言和 disposer。启动时必须恰好注册 69 个可调用 Channel；缺失或多出 Channel 会释放本次注册并失败。手写注销清单已删除，69 个 Channel 对称性测试通过。

### 4.3 P1：会话和附件删除不可恢复

手动删除先执行 `database.deleteConversation()`，再删除附件文件；文件删除失败只记录警告。归档到期清理同样先删除数据库行，再删除附件。数据库引用消失后，系统无法可靠知道哪些文件属于本次失败，也无法进行幂等重试或审计。

代码依据：

- `apps/desktop/src/main/ipc/register-main-ipc.ts:452`
- `apps/desktop/src/main/bootstrap/index.ts:50`
- `apps/desktop/src/main/storage/agent-database.ts:995`

结论：文件系统与 SQLite 无法共享事务，不能假装一次事务解决。应使用“逻辑删除/清理任务 -> 幂等删除无引用文件 -> 最终删除数据库记录”的可恢复流程。

### 4.4 P1：Run 最终提交不原子，状态转换无保护

最终模型内容先由 `appendAssistantTurn()` 写入模型消息、Timeline 和会话时间；随后 `finishRunAndNotifySubagent()` 才更新 Run 终态并投递 Subagent 结果。如果进程在两步之间退出，恢复逻辑可能看到已存在最终回答但 Run 仍为 `running`，并将其当作异常中断。

此外，`updateRun()` 接受任意字符串状态，只检查 Run 是否存在，没有校验合法前置状态。终态 Run 理论上可再次变为 `running`。

代码依据：

- `apps/desktop/src/main/agent/agent-runtime.ts:799`
- `apps/desktop/src/main/agent/agent-runtime.ts:838`
- `apps/desktop/src/main/agent/agent-runtime.ts:1211`
- `apps/desktop/src/main/storage/agent-database.ts:2062`
- `apps/desktop/src/main/storage/agent-database.ts:2867`

结论：数据库层需要一个面向 Run 聚合的终态提交操作；数据库事实提交后再发送内存通知。状态转换必须由显式状态表校验。

### 4.5 P1：Capability 与实际能力不一致（第一批已修复）

初审时项目工具已经暴露 `run_command`、`wait_for_commands` 和 `stop_command`，但 Desktop Capability 仍声明 `process: false`。UI 如果完全按 Capability 控制入口，会隐藏可用能力；如果忽略 Capability，则该合同失去意义。

代码依据：

- `apps/desktop/src/main/ipc/register-main-ipc.ts:101`
- `apps/desktop/src/main/tools/project-tool-registry.ts:288`
- `apps/desktop/src/main/tools/project-tool-registry.ts:455`

处理结果：Desktop `process` 已改为 `true` 并增加合同测试；`pty`、`git`、`mcp`、`skills` 继续保持 `false`，没有误报未完成能力。

### 4.6 P2：核心模块按多个变化原因共同增长

当前规模：

| 文件 | 行数 | 混合职责 |
| --- | ---: | --- |
| `agent-runtime.ts` | 1889 | Agent Loop、上下文、工具派发、重试、消息队列、Subagent/Agent 通知 |
| `agent-database.ts` | 2893 | 77 个公开方法；项目、会话、Run、消息、附件、任务、Subagent、迁移 |
| `project-tool-registry.ts` | 1447 | 工具定义、参数、文件变更、Patch、命令会话、等待和项目操作锁 |
| `register-main-ipc.ts` | 955 | 所有 IPC Channel 注册与适配；注册和释放已由单一事实管理 |
| `model-protocol-adapter.ts` | 870 | 三种协议的请求构造、流解析和状态恢复 |

文件行数不是单独的整改理由。真正的问题是新增一种工具、协议或持久状态时需要修改多个中央分支，且不同职责无法独立做合同测试。

结论：按变化轴拆分，保留现有 Facade 作为迁移边界；禁止一次性大重写。

### 4.7 P2：工具派发没有统一执行合同

AgentRuntime 需要识别 TaskList、Subagent、Agent Communication、Attachment 和 Project 工具族。工具定义、名称判断、参数解析和执行分散在各模块。新增工具族时必须修改 Runtime 中央派发。

结论：引入轻量 `ToolHandler` 注册表是合理的 Registry/Strategy 应用；当前不需要动态插件加载器。

### 4.8 P2：数据库是万能 Repository，迁移没有版本事实

`AgentDatabase` 负责多个聚合和迁移。迁移通过 `PRAGMA table_info` 逐列探测并执行 `ALTER TABLE`，当前没有 `schema_migrations` 版本记录。随着字段增多，无法可靠回答某个数据库已执行到哪一版，也难以覆盖失败恢复。

代码依据：

- `apps/desktop/src/main/storage/agent-database.ts:340`
- `apps/desktop/src/main/storage/agent-database.ts:2291`
- `apps/desktop/src/main/storage/agent-database.ts:2462`

结论：先建立只向前的 Migration Runner，再按事务聚合拆 Repository；不要机械地“一张表一个 Repository”。

### 4.9 P2：模型 Strategy 只完成一部分

OpenAI Chat Completions 已有独立 Adapter；OpenAI Responses、Anthropic Messages 和 Gemini 仍集中在 `ModelProtocolAdapter` 的中央 `switch`。公共 `ModelProviderAdapter`、消息和工具类型又定义在名为 `openai-compatible-adapter.ts` 的具体实现文件，并反向依赖 Storage 类型。

代码依据：

- `apps/desktop/src/main/model/model-protocol-adapter.ts:5`
- `apps/desktop/src/main/model/model-protocol-adapter.ts:421`
- `apps/desktop/src/main/model/openai-compatible-adapter.ts:4`

结论：抽出中立模型合同，每种 API 格式一个 Strategy，由小型 Registry 选择；不要合并各厂商请求和事件语义。

### 4.10 P2：共性底座存在复制和行为漂移

| 共性能力 | 当前位置 | 已出现的差异 |
| --- | --- | --- |
| 路径包含与符号链接校验 | `project-registry.ts`、`configuration-workspace-store.ts` | 后者用 `startsWith("..")`，会把合法的 `..name` 子路径也判为越界 |
| JSON 配置读写 | 4 个 Settings Store，另有模型 Store | 重复默认值、解析、Zod、固定 `.tmp`、重命名和 clone |
| SSE 分帧 | 两个模型 Adapter | 两套 Reader、UTF-8、CRLF、多行 `data:` 和结束刷新逻辑 |
| 工具参数解析 | 4 个工具模块 | Subagent 不把空字符串视为 `{}`，与其他工具不一致 |
| 有界文件预览 | Project 与 Configuration Workspace | 重复字节上限、NUL、严格 UTF-8、二进制和截断判断 |

结论：只抽取容易发生安全或协议漂移的窄函数，保留各领域的授权规则、DTO 和协议解析。禁止建立无所有权的 `common.ts` 或 `utils.ts`。

### 4.11 P2：后端依赖边界没有自动门禁

现有 ESLint `no-restricted-imports` 只作用于 `apps/web`。文档中描述了 Desktop、Agent、Storage、Tool 和 Protocol 依赖方向，但代码没有自动约束，当前已经出现模型合同依赖 Storage 类型的反向耦合。

结论：等目标目录边界稳定后再加 Desktop 受限导入规则；过早加规则只会固化尚未完成的目录迁移。

## 5. 已有设计中应保留的部分

- `packages/protocol` 集中 Zod Schema、IPC Channel 和跨进程 DTO，方向正确。
- IPC 入口统一经过可信窗口检查和错误映射，Renderer 不直接获得 Node 权限。
- 模型请求错误、主进程错误和敏感信息脱敏已经有集中入口。
- SQLite 中若干多表写入已使用 `BEGIN IMMEDIATE` 事务。
- `@vscode/ripgrep` 已作为独立文本搜索实现，文件搜索和文本搜索没有退化成全量 JavaScript 扫描。
- 命令执行具备超时、取消、输出限制、编码处理和 Windows 进程树终止基础。
- 附件、任务清单、Subagent、Agent 消息、队列/Steer 和上下文 Checkpoint 已有明确数据对象，不应在重构中改变用户可见语义。
- 没有提前引入 Java、微服务、消息中间件或动态插件系统，这个克制是合理的。

## 6. 分阶段优化与测试计划

### 6.1 执行规则

- [x] 一次只开始一个批次，不并行进行跨批次重构。
- [x] 每批先补失败复现或 Characterization Test，再修改生产代码。
- [x] 每批只提交该批文件，不混入格式化、命名整理或 UI 改版。
- [ ] 每批自动测试全部通过后，由用户按手工验收项测试。
- [ ] 用户明确确认后，勾选该批“用户确认门”，再开始下一批。
- [x] 任一批出现行为回归，优先回滚该批，不在回归上继续叠加下一批。

### 6.2 第一批：恢复基线、修复 IPC 生命周期和 Capability

目标：先消除确定性错误，不改变 Agent 业务流程。

改动清单：

- [x] 更新 Application Settings 测试夹具和旧配置迁移断言。
- [x] 为 IPC 建立局部 `register()` 收集器，由同一注册事实生成 disposer。
- [x] 新增注册/注销集合一致性测试，覆盖全部 69 个 Channel。
- [x] 将 Desktop `process` Capability 改为与受控命令工具一致。
- [x] 保持 `pty`、`git`、`mcp`、`skills` 为当前真实值，不顺手实现新能力。
- [x] 确认根级 Web Lint 阻断已由独立任务修复，或记录为本批外部阻塞。

禁止顺手修改：Agent Loop、数据库 Schema、工具 UI、设置页布局。

自动验证：

- [x] `pnpm --filter @agent/desktop lint`
- [x] `pnpm --filter @agent/desktop typecheck`
- [x] `pnpm --filter @agent/desktop test`
- [ ] `pnpm lint && pnpm typecheck && pnpm test && pnpm build`

自动验证记录（2026-08-17）：

- 定向测试：Application Settings、IPC 注册器和 Capability 共 3 个测试文件、6/6 通过。
- Desktop 全量：26 个测试文件、166/166 通过；Lint、Typecheck 通过。
- 根级 Typecheck 通过。
- 根级 Test 通过：Protocol 32/32、Web 43/43、Desktop 166/166。
- 根级 Build 通过；Web 主 JS chunk 大于 500 kB 的既有非阻断警告仍存在。
- 根级 Lint 未通过：仅 Web `settings-workspace.tsx` 与 `configuration-workspace-tree-panel.tsx` 存在 3 个 error、2 个 warning；Desktop 与 Protocol 通过，本批未改 Web。
- 手工验收发现模型把内置工具和 Git CLI 误称为 Skill；系统上下文已明确 Skill、工具与命令行程序的边界，并声明当前 Skill Runtime 不可用。Agent Runtime 定向测试 31/31、Desktop 全量 166/166 通过。

手工验收：

- [x] Windows 打开应用、关闭窗口、重新启动，IPC 功能正常。
- [ ] macOS 可用时执行“关闭最后窗口 -> Dock 再次激活 -> 新建窗口”，无重复 Handler 错误。
- [x] UI 能按 Capability 正确显示命令执行能力，PTY/Git/MCP 仍不误报可用。

回滚点：只回滚设置测试、IPC 注册 helper 和 Capability 三组独立提交。

- [x] **用户确认门：第一批手工测试通过，可以开始第二批。**（2026-08-17 用户确认）

### 6.3 第二批：会话与附件可恢复删除

目标：任何文件删除失败或进程退出后，都能从持久状态继续清理。

改动清单：

- [x] 设计并迁移最小删除任务/墓碑表，记录会话、文件引用、状态、重试次数和最后错误。
- [x] 手动删除和归档到期清理共用同一删除 Use Case。
- [x] 先隐藏/标记待删除并写入清理任务，再幂等删除无引用文件，最后删除会话数据并完成任务。
- [x] 启动时恢复未完成清理任务；重复执行不得报错或误删共享快照。
- [x] 文件仍被分叉会话引用时只减少引用，不删除物理文件。

禁止顺手修改：归档 UI、保留天数、附件提取格式、上下文预算。

自动验证：

- [x] 正常手动删除。
- [x] 归档 30 天到期删除。
- [x] 模拟 `EACCES`/文件占用后保留可重试任务。
- [x] 模拟进程在文件删除前后退出并重启恢复。
- [x] 同一快照被父/子会话引用时不误删。
- [x] 清理任务重复执行保持幂等。

自动验证记录（2026-08-17）：

- `ConversationDeletionService` 6/6 通过，覆盖正常删除、共享快照、`EACCES`、任务落库前后重启恢复、重复恢复和归档到期。
- `AgentDatabase` 26/26 通过，覆盖旧数据库新增 `deletion_pending` 与删除任务表、待删隐藏和会话树级联收尾。
- `ConversationAttachmentStore` 3/3 通过；Desktop 全量 27 个测试文件、173/173 通过，Lint、Typecheck 通过。
- 根级 Typecheck、Test、Build 通过：Protocol 32/32、Web 43/43、Desktop 173/173；根级 Lint 仍只被既有 Web 3 个 error、2 个 warning 阻断。
- 已正常关闭旧 Desktop，使 WAL 完成 checkpoint；升级前数据库备份到 `C:\Users\gyx\AppData\Roaming\@agent\desktop\backups\pre-conversation-deletion-20260817-191256\agent.sqlite`。
- 新版 Desktop 使用真实开发库启动成功，Renderer `http://127.0.0.1:5173` 返回 200；`conversations.deletion_pending` 与 `conversation_deletion_tasks` 均已创建，启动后待删会话和未完成删除任务均为 0。
- 手工验收期间发现侧边对话被错误标识为 Subagent，且没有复制父对话最新 Context Checkpoint。已将侧边 Fork 与任务 Subagent 分流，并补齐 Checkpoint 序号映射、无压缩完整历史、已绑定 Agent 配置、UI 隐藏继承消息和历史身份迁移测试。
- 本次定向验证：`AgentDatabase` 27/27、侧边 Fork Runtime 1/1、Web Mock Client 16/16 通过；Desktop/Web Typecheck 与 Desktop Lint 通过。受限执行环境禁止测试进程 `spawn`，Desktop 全量中 158/175 通过，17 项命令/ripgrep 用例均因 `spawn EPERM` 阻断，不是本次上下文断言失败；Web 全量 43/43 通过。
- 真实库副本升级演练：`PRAGMA quick_check = ok`，8 个无任务事实的历史侧边对话迁移为普通 Agent，3 个有 `subagent_tasks` 的真实 Subagent 保持不变，误分类为 0。当前受限环境无法写入真实 `AppData`，真实用户库迁移和模型端到端对话仍需在正常桌面权限下验收。

手工验收：

- [x] 创建带文本、图片附件的会话，删除后数据库和目录均清理。
- [x] 人为锁定附件制造失败，解除占用并重启后自动完成清理。
- [x] 归档、恢复、再次归档的时间和删除行为正确。

回滚点：Migration 只向前；代码可切回旧调用入口，但不得删除已创建的删除任务表。

- [x] **用户确认门：第二批手工测试通过，可以开始第三批。**（2026-08-17 用户确认）

### 6.4 第三批：Run、最终消息和 Subagent 结果原子终态

目标：数据库中不存在“已有最终回答但 Run 仍运行”或非法回退状态。

改动清单：

- [x] 定义 Run 合法状态转换表和终态集合。
- [x] 新增 Run 聚合提交方法，在一个 SQLite 事务内写最终消息、Timeline、Run 终态、未读状态和可持久化的 Subagent 结果。
- [x] 数据库提交成功后再发送 EventEmitter/内存通知。
- [x] 重复完成、终态回退、错误 Run ID 返回稳定错误。
- [x] 启动恢复只处理中间状态，不覆盖已经原子完成的 Run。

禁止顺手修改：模型重试规则、消息渲染、Subagent 提示词、任务路由。

自动验证：

- [x] completed/failed/cancelled 的合法转换。
- [x] 终态不能回到 running。
- [x] 在事务各写入点注入异常时整体回滚。
- [x] Subagent 结果只投递一次，父对话收到摘要和来源 ID。
- [x] 重启恢复不会把已完成回答标记失败。

自动验证记录（2026-08-18）：

- `agent-database.test.ts` 34 项通过，覆盖终态状态机、最终消息/Run/Subagent 聚合提交、五个 SQLite 写入点故障注入回滚及重启恢复。
- `agent-runtime.test.ts` 的终态提交顺序、Subagent 等待和后台完成唤醒定向测试通过；完整 Runtime 测试中与本批无关的 PowerShell 子进程测试受当前环境 `spawn EPERM` 阻断。
- 2026-08-18 补充取消流式回归：Runtime 定向测试 35 项通过，验证已收到的 Assistant 文本以 `cancelled` 状态持久化，Subagent `task_result` 不进入父 Timeline。
- 2026-08-18 补充父对话运行态回归：父 Run 已结束但仍有 `queued/running` Subagent 时，`activeSubagentCount` 保持大于零；Subagent 完成后计数归零并自动重新激活父 Agent。全仓 Protocol 34/34、Web 44/44、Desktop 186/186 测试通过，Protocol/Desktop Lint、全仓 Typecheck 和 Web Build 通过。
- 本次桌面端已重启加载最终构建；根级 Web Lint 仍被设置工作区与配置文件树的既有 3 个 error、2 个 warning 阻断，未纳入本批。

手工验收：

- [ ] 普通对话、工具对话、取消对话均正确结束。
- [ ] 创建 Subagent，完成后父对话被激活，且子对话仍可打开查看。
- [ ] 强制结束应用并重启，不出现重复回答或错误未读状态。

回滚点：保留旧 Facade 方法一批版本；新聚合方法稳定后再删除旧调用。

- [ ] **用户确认门：第三批手工测试通过，可以开始第四批。**

### 6.5 第四批：安全与 I/O 公共底座

目标：统一已经发生行为漂移的窄基础能力，不改变领域授权策略。

改动清单：

- [x] 提取 `isPathInsideRoot`、现有路径解析和可写父目录校验原语。
- [x] Project 与 Configuration Workspace 继续保留各自允许操作的业务规则。
- [x] 提取 `readBoundedFilePreview()`，统一二进制、UTF-8 和截断语义。
- [x] 提取组合式 JSON 配置读写函数，统一临时文件唯一命名、失败清理、Zod 校验和错误上下文。
- [x] 逐个 Store 迁移；每迁移一个先跑其现有测试。

禁止顺手修改：DTO 结构、最大预览字节、配置字段和默认值；不创建 `utils.ts`。

自动验证：

- [x] `..`、绝对路径、目录符号链接、文件符号链接和 Windows 大小写边界。
- [x] 合法 `..name` 子路径不被误判。
- [x] 空文件、NUL、非法 UTF-8、恰好上限和超过上限的预览。
- [x] 配置文件不存在、损坏、Schema 失败、并发保存和 rename 失败。
- [x] 迁移前后的所有 Store Characterization Test 等价。

自动验证记录（2026-08-18）：

- 新增路径底座测试 4/4，覆盖 `..`、绝对路径、合法 `..name`、Windows 大小写、目录/文件符号链接和可写父目录。
- 新增有界预览测试 3/3，覆盖空文件、NUL、非法 UTF-8、恰好上限和超过上限；项目和配置工作区原有预览行为保持不变。
- 新增 JSON 配置底座测试 4/4，覆盖缺失默认值克隆、损坏 JSON、Schema 失败、并发保存唯一临时文件和 rename 失败清理。
- Desktop 定向受影响测试 38/38；Desktop 全量 30 个测试文件、197/197；Protocol 34/34；Web 45/45。
- Desktop/Protocol Lint、全仓 Typecheck、全仓 Build 通过。根级 Lint 仍被既有 Web 设置工作区 3 个问题（2 error、2 warning）和配置文件树 1 个 error 阻断，本批未修改。

手工验收：

- [ ] 项目树和配置工作区能正常创建、读取、编辑和删除允许的文件。
- [ ] 越界和受保护文件操作仍被拒绝，错误信息可理解。
- [ ] 设置重启后保持，写入失败不破坏上一次有效配置。

回滚点：每个底座函数和调用方迁移分开提交，可按 Store 回退。

- [ ] **用户确认门：第四批自动验证完成，待用户手工测试后可以开始第五批。**

### 6.6 第五批：统一 SSE、工具参数与 ToolHandler 注册表

目标：Runtime 不再知道每个工具族的具体类，同时保持所有工具名称、Schema 和 UI 结果不变。

改动清单：

- [x] 提取 `readSseDataStream()` 并让两套模型 Adapter 共用。
- [x] 提取 `parseToolArguments()`，统一 `""` 与 `"{}"` 行为，再交给各工具 Zod Schema。
- [x] 定义最小 `ToolHandler`：定义、名称判断、执行；不加入动态加载。
- [x] 将 TaskList、Subagent、Agent Communication、Attachment、Project 工具注册为 Handler。
- [x] Project Facade 保留读取/搜索、文件变更、命令生命周期的现有私有分组；这些方法共享项目操作锁和命令会话状态，本批不做无收益的跨文件拆分。
- [x] Runtime 只遍历 Handler Registry，不再增加工具族 `if/else`。

禁止顺手修改：工具名称、输入 Schema、审批语义、输出卡片、模型 Prompt。

自动验证：

- [x] SSE 覆盖 UTF-8 分块、CRLF、多行 `data:`、无空行 Provider、`[DONE]` 和尾块。
- [x] 所有无参数工具接受空字符串和 `{}`，非法 JSON 统一失败。
- [x] 每个 Handler 有定义/执行合同测试，未知工具稳定报错。
- [x] 文件读可并发，项目写/命令冲突锁语义保持。
- [x] 全部既有 AgentRuntime 工具场景通过。

自动验证记录（2026-08-18）：

- 新增 SSE 合同测试 2/2，覆盖 UTF-8 多字节跨块、CRLF、多行 `data:`、无空行 Provider、`[DONE]` 和尾块。
- 新增工具参数测试 2/2，并迁移 TaskList、Subagent、Agent Communication、Attachment、Project 和模型协议适配器；空字符串与 `{}` 统一归一为空对象，非法 JSON 统一失败。
- 新增 ToolHandler Registry 合同测试 3/3，覆盖定义唯一性、可用性过滤、执行派发和未知工具稳定错误。
- Desktop 全量 33 个测试文件、208/208；Protocol 34/34；Web 45/45。Desktop/Protocol Lint、全仓 Typecheck 和 Build 通过。
- 根级 Lint 仍被 Web `workspace-content.tsx` 1 个 purity error、设置工作区 2 个 error/2 个 warning、配置文件树 1 个 error 阻断；均为本批外部 Web 改动，本批未修改。

工具合同补充记录（2026-08-18）：

- 所有内置工具的模型参数继续由执行端 Zod Schema 生成；新增四种 Model Adapter 的工具 Schema 序列化合同测试，确认 `$schema` 不会发送给 Provider。
- `parseToolArguments()` 对非法 JSON 和非对象输入返回稳定的 `TOOL_ARGUMENTS_INVALID` 结构化错误；恢复包的 issue 数量、路径、代码和消息均有界，超长异常不会破坏 `AgentError` 协议。
- `read_file` 的跨字段行号范围改由执行端 Zod `superRefine` 校验；逆序或超过 400 行时返回 `endLine` 字段级恢复 issue，避免模型只能看到笼统异常。
- 项目搜索、文件写入、文本替换和命令等待参数补充了边界与副作用说明，Schema 与执行行为保持同源。
- 新增定向验证：工具参数、错误分类、恢复包、行范围跨字段校验和四种 Adapter Schema 序列化合同测试通过；未调用真实 Provider 端点，在线兼容性仍需手工验收。

手工验收：

- [ ] 分别触发读取、搜索、写入、Patch、命令、任务清单、Subagent 和 Agent 消息。
- [ ] 工具卡片类型、展开内容、错误状态和审批行为没有变化。
- [ ] 连续流式对话和工具调用不丢块、不重复块。

回滚点：Registry 先作为旧派发外层；合同测试稳定后再删除旧中央分支。

- [ ] **用户确认门：第五批自动验证完成，待用户手工测试后可以开始第六批。**

### 6.7 第六批：Migration Runner 与数据库职责拆分

目标：建立可追踪 Schema 版本，并按事务边界缩小 `AgentDatabase`。

改动清单：

- [x] 新增 `schema_migrations` 和只向前 Migration Runner。
- [x] 为当前无版本数据库建立一次性基线识别，不能重复执行已有列迁移。
- [ ] 先拆 `RunRepository`，再拆 Conversation/Attachment、Task/Subagent；跨聚合事务由显式 Unit of Work 协调。
- [ ] `AgentDatabase` 暂时保留为 Facade，调用方分批迁移。
- [ ] SQL Row 转领域对象的映射归所属 Repository，不在 Runtime 格式化。

禁止顺手修改：表名、业务状态、删除策略、上下文算法；不按单表机械拆类。

自动验证：

- [x] 空库初始化、当前旧库升级、重复启动、部分 Migration 失败回滚。
- [x] 迁移版本只能前进，未知未来版本拒绝启动并给出可诊断错误。
- [ ] Repository 合同测试和跨聚合事务回滚。
- [ ] 使用复制的真实开发库做只读备份后升级演练。

手工验收：

- [ ] 使用全新用户目录首次启动。
- [ ] 使用升级前数据库启动并查看项目、会话、任务、附件和 Subagent。
- [ ] 连续重启两次不重复迁移、不丢数据。

回滚点：迁移文件不可改写；若失败，恢复升级前数据库备份和上一版应用。

- [ ] **用户确认门：第六批手工测试通过，可以开始第七批。**

第六批实施记录（2026-08-18）：

- 新增 `DatabaseMigrationRunner`，以 `schema_migrations(version, name, applied_at)` 记录已应用版本；版本必须严格递增且只向前执行，未来版本会在启动时拒绝。
- 将现有数据库结构和历史补列逻辑作为版本 1 的一次性基线迁移；迁移在 `BEGIN IMMEDIATE` 中执行，失败回滚并不写入版本记录。构造失败时关闭数据库句柄，避免 Windows WAL/SHM 锁残留。
- `AgentDatabase` 保留 Facade 和现有事务/业务方法，未在本批拆分 Repository，避免改变跨聚合事务边界。
- 自动验证：Desktop 34 个测试文件、215/215 通过；Desktop lint、typecheck 通过。Migration Runner 单测覆盖重复启动、失败回滚、未来版本、版本顺序和缺洞历史；旧库升级夹具覆盖项目/临时会话、归档字段、删除任务和 Agent 身份。
- 尚未完成：`RunRepository` 等职责拆分、复制真实用户库的只读升级演练、手工首次启动/连续重启验收。

### 6.8 第七批：模型 Adapter Strategy 完整化

目标：新增协议不修改已有协议实现，公共合同不依赖 Storage。

改动清单：

- [x] 将 `ModelProviderAdapter`、Message、ToolCall、ProviderState 移到中立合同文件。
- [x] OpenAI Chat、OpenAI Responses、Anthropic、Gemini 各自成为独立 Adapter。
- [x] 使用小型 Adapter Registry 按 `apiFormat` 选择实现。
- [x] 公共只保留 HTTP/SSE/图片说明/错误映射原语，各协议请求体和事件解析不合并。
- [x] 保持 Provider 配置、模型快照和推理程度语义不变。

禁止顺手修改：设置 UI、模型目录、重试次数、请求参数命名、供应商特有字段。

自动验证：

- [x] 四种协议的请求快照、流式文本、工具调用、图片和 Provider State 恢复。
- [x] 非 2xx、鉴权、限流、超时、断流和非法事件统一映射。
- [x] Registry 未注册格式稳定失败。
- [x] 现有模型 Adapter 测试全部迁移且数量不减少。

手工验收：

- [ ] 使用已配置的测试端点逐种协议完成普通多轮对话和工具调用。
- [ ] 切换模型、思考程度和上下文窗口后只影响后续 Run。
- [ ] 错误提示显示在对应 Assistant 输出位置。

回滚点：每个协议单独迁移并提交；任一协议可暂时回退到旧 Adapter。

- [ ] **用户确认门：第七批手工测试通过，可以开始第八批。**

第七批实施记录（2026-08-18）：

- 新增 `model-contracts.ts`，模型配置、消息、附件、工具、Provider State 和 Adapter 合同不再从 Storage 或 Credential Store 反向定义；Storage 仅复用中立类型并保留兼容导出。
- 将四种协议收敛为独立策略类：`OpenAiChatCompletionsAdapter`、`OpenAiResponsesAdapter`、`AnthropicMessagesAdapter`、`GoogleGeminiAdapter`；保留 `OpenAiCompatibleAdapter` 兼容别名，旧调用方行为不变。
- 新增 `ModelAdapterRegistry`，生产默认注册四种格式，未注册格式返回稳定错误；Agent Runtime 和模型连接测试改用 Registry，不在 Runtime 判断供应商。
- 自动验证：Desktop 35 个测试文件、218/218 通过；模型协议与 Registry 定向测试通过；Desktop lint、typecheck 通过。
- 尚未完成：四种协议使用真实已配置端点的手工多轮/工具调用验收；第八批依赖门禁和长期并发/性能回归尚未开始。

### 6.9 第八批：后端依赖门禁与长期回归

目标：把已稳定的边界变成自动检查，防止架构再次回退。

改动清单：

- [x] 为 Desktop 增加 `no-restricted-imports`，固化 Protocol、Application、Storage、Tool、Model 的依赖方向。
- [x] 增加架构测试：IPC 注册对称、Tool 名唯一、Adapter 格式唯一、Migration 版本连续。
- [x] 增加关键并发/性能基线：并行读、串行冲突写、长命令输出、SSE 高频块、长对话上下文构建。
- [ ] 补 Electron 打包冒烟与窗口重建测试记录。
- [x] 更新本审查计划、后端规范和业务上下文中的实现状态。

禁止顺手修改：拆包到多个进程、Worker Pool、插件市场或远程 Agent Server。

自动验证：

- [ ] `pnpm lint && pnpm typecheck && pnpm test && pnpm build`
- [ ] Windows Electron 启动/退出/重启冒烟。
- [ ] 数据库迁移与崩溃恢复集成测试。
- [ ] 工具并发和资源释放测试无悬挂进程、句柄或临时文件。

手工验收：

- [ ] 完整走通项目对话、临时对话、附件、多轮、任务清单、Subagent、Agent 消息和归档恢复。
- [ ] 对照第一阶段验收文档执行一次完整回归。
- [ ] 用户确认没有旧功能回归后，关闭本轮后端治理计划。

回滚点：Lint 规则和架构测试独立提交；规则不应通过大面积禁用来“修绿”。

- [ ] **用户确认门：第八批完成，本轮后端治理结束。**

第八批实施记录（2026-08-18）：

- ESLint 增加 Desktop Model/Storage 的受限导入边界；模型只能依赖中立合同和协议公共原语，Storage 不得依赖 Runtime/Tool 编排。规则未通过禁用项绕过，现有 Desktop lint 通过。
- 新增架构合同测试，检查模型边界和四种 Adapter Registry 显式注册；已有 IPC 注册/释放对称、Tool 名唯一、Migration 版本连续测试纳入回归。
- 新增高频 SSE 2,000 帧顺序与完整性基线；项目工具已有并发读、串行冲突写、长命令输出和资源释放测试，Subagent/Agent 消息并发测试继续覆盖。
- 自动验证：Desktop 36 个测试文件、221/221 通过；Desktop lint、typecheck 通过，Desktop build 通过。
- 全仓门禁状态：Protocol/Web 测试通过，root `typecheck/build/lint` 仍受工作树中既有 Web `workspace-content.tsx`、settings 和 configuration panel 错误阻断，未混入本阶段修改。
- 尚未完成：Electron 真启动/退出/窗口重建冒烟、真实用户库升级演练、全量 Web 基线修复和用户最终回归。

统一异常展示补充记录（2026-08-18）：

- 模型请求错误按网络、认证、额度/限流、超时、上游、响应和内部错误分类；工具错误留在对应工具行，普通界面操作错误留在工作区操作区域。
- 模型和普通操作错误使用 md-king 风格静态引用块；工具错误留在对应工具行，工具卡片自身仍可折叠查看原始调用。引用块直接显示脱敏的 HTTP 状态、供应商摘要、网络/内部技术详情或工具错误。内部错误 UUID、request/trace/correlation id 不进入用户文案和原始 Agent 错误查看窗口。
- `model.retry_updated` 携带持久 `model_retry` Timeline 事实和脱敏重试原因；HTTP 502 等上游错误不会因摘要文字出现 `gateway timeout` 被误判为超时，只有 408/504 或无状态明确超时文本才归超时。
- 自动验证：Protocol 37/37、Web 55/55、Desktop 228/228；Protocol/Web/Desktop Typecheck、Desktop Lint、Web 定向 Lint 和 Web/Desktop Build 通过；`git diff --check` 通过。
- 全量 `pnpm lint` 仍被设置工作区与配置文件树既有 3 个 error、2 个 warning 阻断，未混入本补充；未调用真实模型端点。
- 待手工验收：分别触发模型认证/限流/网络/上游错误、命令失败和文件变更失败，确认静态引用块、归属位置、详情脱敏和重试提示。

### 6.10 第九批：LangChain/LangGraph、Skill 上下文与图 Checkpoint

目标：让框架接管 Agent 内部图循环和模型协议，同时保留 `AgentRuntime` 应用 façade、业务 SQLite 事实和工具权限边界。

> 历史记录：本批结束时审批 `interrupt/Command` 和官方 `ToolNode` 两项尚未完成，已在下方第十批补齐；保留原批次状态用于追溯。

改动清单：

- [x] 引入 `@langchain/core`、四个 Provider 包、`@langchain/langgraph` 和 Checkpoint 合同；删除旧 AI SDK/手写 Provider 生产适配器。
- [x] 第九批先用 `StateGraph` 接管 `model -> tools -> model` 循环、步骤上限、取消传播、工具结果和 Skill 快照状态；生产主链后续已在第十三批替换为 LangChain `createAgent`。
- [x] 实现基于 `node:sqlite` 的 `NodeSqliteCheckpointSaver`，支持父链、metadata、pending writes、重启读取和 thread 删除；不引入 `better-sqlite3`。
- [x] Skill 按目录摘要 -> `load_skill` -> `SKILL.md` 正文 -> 受控 `references/templates` 渐进加载；正文不写入聊天 Timeline，快照保存版本和 SHA-256。
- [x] 会话删除任务纳入 Graph thread 清理；Checkpoint 清理失败与附件清理共用可恢复重试任务。
- [x] 应用重启自动恢复尚未进入模型/工具节点的 queued Run；running Run 不自动重放，按潜在副作用标记失败。
- [ ] 以 LangGraph `interrupt/Command` 直接承接逐次审批；当前审批仍由 Runtime 等待器管理。
- [ ] 用官方 `ToolNode`/`StructuredTool` 取代当前 Runtime callback wrapper；当前 wrapper 保留项目审计、权限和事件合同。

自动验证记录（2026-08-19）：

- 本批 7 个核心测试文件定向验证 134/134：Agent Runtime 43/43、Skill Runtime 12/12、LangGraph Executor 4/4、LangChain 模型 Adapter 27/27、Agent Database 37/37、删除服务 8/8、SQLite Saver 3/3；覆盖 Runtime Skill 集成、临时工作区不放行 project-scoped Skill、禁用 Agent fail-closed、Skill 变更拒绝、共享保守 Token 估算、Graph 父链重开、queued Run 重启恢复和 Checkpoint 清理重试。
- Desktop 全量 39 个测试文件、264/264 通过；Desktop lint、typecheck 通过。
- 全仓 `pnpm typecheck`、`pnpm test`（Protocol 37/37、Web 55/55、Desktop 264/264）和 `pnpm build` 通过；根 `pnpm lint` 仍被既有 Web `settings-workspace.tsx` 的 2 个 error/2 个 warning 与 `configuration-workspace-tree-panel.tsx` 的 1 个 error 阻断，本批未修改这些 Web 文件。
- 模型 Adapter 合同已验证 Anthropic URL、Google API 版本、多模态附件、OpenAI Responses 工具参数、Provider State 兼容、错误脱敏和取消传播；Skill 正文只在模型上下文注入，授权范围和上下文恢复均有回归覆盖。
- Skill 依赖 MCP 的条目在 MCP Runtime 未实现时不会进入目录；Skill 脚本执行、running Run 自动恢复和真实 Provider 端点验收仍未完成，Capability/业务状态不能标为“全部完成”。

手工验收：

- [ ] Windows Electron 强制退出/重启，验证 queued Run 只执行一次、running Run 不重复写文件或命令。
- [ ] 真实 Skill 文档、reference 和模型多轮调用；确认正文只在模型上下文出现，UI Timeline 不出现正文。
- [ ] 删除带 Graph Checkpoint 的会话，验证文件占用或 Checkpoint 锁定后任务可重试。

回滚点：`268454a` 技术方案文档和本批代码保持独立；若框架适配回归，回退本批但保留之前已确认的业务数据库/删除任务迁移。

### 6.11 第十批：ToolNode、审批中断恢复与 Skill 预算

完成日期：2026-08-19

本批把第九批遗留的框架边界落到生产主链：

- [x] 用 LangGraph `interrupt()` 暂停文件变更和命令审批，用 `Command({ resume })` 按 interrupt namespace 恢复；审批决定只消费一次，取消和异常会清理 pending approval。
- [x] 用 LangChain `DynamicStructuredTool` 与 LangGraph `ToolNode` 接管工具名称分发和 ToolMessage 转换；现有 `ToolHandlerRegistry` 继续负责 Zod 参数、权限、项目工作区、审计、事件和副作用。
- [x] 缓存同一 `runId + providerCallId` 已完成 ToolCall 的结果。节点因后续审批中断重放时只恢复 ToolMessage，不再次执行写文件、命令或其他副作用，也不重复 `tool.started`。
- [x] ContextManager 在历史裁剪前预留 Skill 正文预算，`SkillRuntime` 使用同一预算注入正文；预算纳入上下文用量的 `estimatedSystemTokens`，Skill 正文仍不进入 Timeline。
- [x] 增加双 interrupt、多个待审批 ToolCall、已完成副作用不重放、Skill 预算和桌面主链回归测试。
- [x] 非取消、非审批中断的工具适配器异常统一转换为失败 `ToolMessage`，持久化工具 `status=failed` 并发送一次 `tool.completed`；取消和审批中断仍由 LangGraph 原样处理。

自动验证记录（2026-08-19）：

- 桌面 Agent/Skill/Context/LangGraph 定向测试：4 个测试文件、66/66 通过；桌面全量测试 39 个文件、268/268 通过。
- Desktop build、lint、typecheck 通过；根 `pnpm typecheck` 通过；根 `pnpm test` 通过（Protocol 37/37、Web 55/55、Desktop 268/268）；根 `pnpm build` 通过。
- 根 `pnpm lint` 仍被既有 Web lint 阻断：`settings-workspace.tsx` 2 个 `react-hooks/set-state-in-effect` error、2 个依赖 warning，`configuration-workspace-tree-panel.tsx` 1 个 `no-control-regex` error；本批未修改这些文件。
- 真实 Provider、Electron 强制重启和真实 Skill 文档手工验收仍是后续证据。

当前边界：LangGraph 负责图状态、路由、ToolNode 调度、interrupt/resume 和 Checkpoint；`AgentRuntime` 仍负责业务 SQLite、IPC、Queue/Steer、Agent/Subagent 通信、权限策略、路径安全、进程树、审计和最终事件。跨 Agent 通信不迁移到 Graph State，继续使用持久化 `AgentDatabase` 合同。

### 6.12 第十一批：多 Tool Call 调度与批次文件版本保护

完成日期：2026-08-20

目标：让模型一轮返回的多个 `toolCalls` 真正获得安全的批量执行能力，同时不把可并发读取误扩展成副作用并发。

改动清单：

- [x] 为 `ToolHandlerRegistry` 增加显式调度策略；只读、默认并行命令和有序副作用由各 Handler 声明，Runtime 不再对所有 Tool Call 使用同一并发策略。
- [x] 连续的安全只读调用交给 LangGraph `ToolNode` 并行执行，使用 `tool_call_id` 关联结果并按模型原始顺序生成 `ToolMessage`。
- [x] 非 `read_only` 下独立 `run_command` 默认并行，`parallel=false`、等待/停止和 Agent 消息保持有序；询问模式逐条审批后进入并行窗口，并以运行中 ToolCall 缓存避免 interrupt 重放副作用。
- [x] 同一模型轮的文件变更在首个审批/写入前预先准备，固定每个调用的 `expectedContent`；同文件后续旧调用返回 `FILE_CHANGED`，不静默重新基线化。
- [x] 明确多文件变更仍是逐文件审批/写入的非原子批次，不新增回滚事务或 Worktree 隔离。
- [x] 增加批量读取、并行命令、同文件过期变更和 Handler 调度策略合同测试。

自动验证记录（2026-08-20）：

- Desktop 定向测试：4 个文件、112/112 通过，覆盖 Agent Runtime、Project Tool Registry、Tool Handler Registry 和 LangChain Model Adapter。
- Desktop lint、typecheck 通过。
- Desktop 全量测试：39 个文件、278/278 通过。
- 全仓 typecheck、test、build 通过；测试为 Protocol 37/37、Web 55/55、Desktop 278/278。Web build 仍有既有大分块提示。
- 根级 lint 仍被未纳入本批的 Web 设置页和配置工作区树 3 个 error、2 个 warning 阻断；Desktop 与 Protocol lint 通过。
- 尚未完成：真实 Provider 多 Tool Call 端点验收和 Electron 手工 UI 验收。

当前边界：LangGraph 负责安全并发组的 ToolNode 执行和 ToolMessage 转换；项目 Handler 仍负责权限、审批、项目冲突、文件版本和副作用生命周期。历史版本曾要求模型显式声明且只在完全放开权限下并行；该限制已在第十五批修正为非 `read_only` 模式下独立命令默认并行，`parallel=false` 显式串行，询问模式逐条审批后仍进入并行窗口；多文件不是事务。

### 6.13 第十二批：Tool Call 数量与并发宽度保护

完成日期：2026-08-20

目标：为批量 Tool Call 增加确定性资源边界，避免异常模型响应无限放大本地文件 I/O、进程、审批和 Timeline 写入。

改动清单：

- [x] 单个模型轮最多接受 32 个 Tool Call；超限时整批在任何工具启动前失败，不截断执行前缀。
- [x] 连续只读调用按最多 8 个一组交给 LangGraph `ToolNode`；超出部分保持原顺序进入下一组。
- [x] 非 `read_only` 下的独立命令按最多 4 个进程一组执行；`parallel=false` 保持串行，询问模式只串行等待审批，不串行化已批准的独立命令。
- [x] System Prompt、工具设计、接口合同、业务上下文和后端规范同步记录同一组限制。
- [x] 增加 33 个调用整批拒绝、10 个读取并发宽度和 6 个命令并发宽度的 Runtime 回归测试。

自动验证记录（2026-08-20）：

- Desktop 定向测试：新增 3/3 通过；错误、Handler 和 Project Tool Registry 相关测试 41/41 通过。
- Desktop lint、typecheck 通过；全量测试 39 个文件、279/279 通过。
- 全仓 typecheck、test、build 通过；测试为 Protocol 37/37、Web 55/55、Desktop 279/279。Web build 仍有既有大分块提示。
- 根级 lint 仍被未纳入本批的 Web 设置页和配置工作区树 3 个 error、2 个 warning 阻断；Desktop 与 Protocol lint 通过。
- 尚未完成：真实 Provider 超限响应和大批量 Tool Call 的 Electron 手工 UI 验收。

### 6.14 第十三批：`createAgent` 生产主链与框架能力收口

完成日期：2026-08-21

目标：让 LangChain 当前高层 Agent API 接管标准循环和通用执行控制，只保留框架无法满足的项目业务合同，避免长期维护第二套 Agent Loop。

改动清单：

- [x] 生产 `LangGraphExecutor` 改用 `langchain@1.5.9` 的 `createAgent + createMiddleware`；删除手写 `StateGraph` model/tools 条件循环。
- [x] 最大模型调用次数改用框架 `modelCallLimitMiddleware`，按应用生成的 Run `thread_id` 持久计数；Runtime 只传入上限并映射稳定错误。
- [x] 保留 `version: "v1"`，让同轮 Tool Call 作为完整批次进入 Runtime；Runtime 再按只读 8、非只读模式下默认并行命令 4、其余有序的业务策略交给 ToolNode 执行。
- [x] Queue/Steer 在无工具结束边界使用同一 Graph thread 再次 invoke，确保每次都经过 `beforeModel` 注入持久化输入。
- [x] 同轮重复和跨上下文复用的 `toolCallId` 均在新工具启动前拒绝；未知工具持久化失败 Tool 行并返回 ToolMessage，让模型可继续修正。
- [x] 更新 LangGraph 方案、工具生命周期和后端规范，记录 `createAgent` 当前架构及框架采用/不采用矩阵。

当前边界：`createAgent`/LangGraph 负责模型工具循环、ToolNode、模型调用上限、Checkpoint 和 interrupt/resume；Runtime 继续负责权限、审批业务事实、文件版本、项目冲突、流式可观测重试、上下文/Skill 预算、业务 SQLite、Subagent 和跨 Agent 消息。LangChain 内置摘要、通用 HITL、模型重试和内存式 Subagent 不满足这些现有合同，因此没有为追求“全框架”而重复接入。

自动验证记录（2026-08-21）：

- 新增定向回归：2 个测试文件、3/3 通过（另有 58 项因 `-t` 过滤跳过），覆盖完整 Tool 批次只进入 Runtime 一次、同轮重复 ID 和跨轮 ID 复用。
- Desktop lint、typecheck 通过；Desktop 全量测试 39 个文件、291/291 通过，无跳过或失败。
- 全仓 lint、typecheck、test、build 通过；测试为 Protocol 37/37、Web 55/55、Desktop 291/291，共 383/383。
- Build 只有既有 Web 单分块超过 500 kB 提示；测试过程只有 Node `sqlite` experimental warning，不影响结果。
- 尚未完成：真实 Provider 多 Tool Call、Electron 强制退出/queued Run 恢复、真实 Skill 文档与 UI 审批闭环手工验收。

### 6.15 第十四批：模型重试 Middleware 与 `beforeAgent` 上下文边界

完成日期：2026-08-22

目标：继续把通用执行控制交给 LangGraph，同时保持 Runtime 对模型错误分类、上下文预算、业务事实和流式副作用语义的所有权。

改动清单：

- [x] 将主 Agent 模型请求的重试循环移入 `LangGraphExecutor` 自定义 `wrapModelCall` Middleware；Runtime 仅提供重试判定、退避时间、可取消等待、脱敏重试事件和最终失败回调。
- [x] 保留“已产生文本不重试”、空响应重试和成功工具后的空响应语义；重试尝试不增加 Graph 模型回合计数，也不重复写入 Assistant Timeline。
- [x] 将首次 `prepareContext`/压缩接入 `createAgent.beforeAgent`，以 `contextPrepared` 状态保证同一 `thread_id` 的 Queue/Steer 和审批恢复不重复追加历史；Queue/Steer 仍在 `beforeModel` 消费。
- [x] 增加 Graph 重试、流式失败不重放、`beforeAgent` 单次初始化和后续输入回归测试。

自动验证记录（2026-08-22）：

- LangGraph/Agent Runtime 定向测试：2 个文件、64/64 通过。
- Desktop lint、typecheck 通过。
- Desktop 全量测试：41 个测试文件、312/312 通过；根 `pnpm lint` 和 `pnpm test` 通过。
- Desktop build 通过；根 `pnpm typecheck` 和 `pnpm build` 被当前 Web 文件 `apps/web/src/features/settings/provider-logo.tsx:64` 的 `TS18048: 'option' is possibly 'undefined'` 阻断，本批未修改该无关文件。
- Electron 强制退出恢复、真实 Provider 流式重试和真实 Skill 文档/UI 审批闭环尚未手工验收。

当前边界：LangGraph 负责模型重试循环、Graph 状态和通用路由；Runtime 继续负责重试策略内容、上下文选择/预算、权限审批、业务持久化、Queue/Steer 和跨 Agent 通信。上下文压缩模型调用仍是 Runtime ContextCompactor 的独立服务，不伪装成 Agent 图中的工具回合。

### 6.16 第十五批：真实并行 Tool Call 与审批恢复幂等

完成日期：2026-08-22

目标：确保模型一次返回多个独立 Tool Call 时，命令和只读工具在后端真实并发执行，而不是只在 Renderer 标记为“并行”。

改动清单：

- [x] `run_command.parallel` 默认值为 `true`；非 `read_only` 模式下同一连续独立命令组按最多 4 个并发，显式 `parallel=false` 保持串行。
- [x] `ask_before_changes` 仍逐条请求审批，但已批准的独立命令进入同一并发窗口；不因审批流程把已批准命令重新串行化。
- [x] 对每个 `runId + toolCallId` 增加运行中 Promise 和已完成结果缓存。LangGraph interrupt 恢复重放 ToolNode 时复用结果，不重复启动命令、文件写入或其他副作用。
- [x] OpenAI Chat Completions/Responses 请求显式发送 `parallel_tool_calls: true`，使 Provider 不主动降级为单工具调用。
- [x] Timeline 保存 `executionMode` 和批次 ID；Renderer 将同一并发批次显示为“并行执行中/并行执行”。

自动验证记录（2026-08-22）：

- Desktop 定向并行与 Provider 测试：4 个文件、134/134 通过；覆盖 6 条命令最大并发 4、10 条读取最大并发 8、混合 Tool Call 的安全顺序屏障、询问模式每条命令只执行一次、OpenAI 两种 API 格式的 `parallel_tool_calls` 请求字段。
- Desktop lint、typecheck、test、build 通过；Desktop 全量 41 个测试文件、319/319 通过（含 10 个文件审批批次的递归上限回归）。
- Web lint、typecheck、test、build 通过；Web 全量 31 个测试文件、143/143 通过。
- 根 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；Build 仅保留既有大分块提示，测试仅有 Node SQLite experimental warning。

当前边界：模型必须先返回多个 Tool Call，Runtime 才能并发；Provider 或模型若只返回单个调用，后端不会擅自拆解一个命令。文件写入、等待/停止、消息投递和任务状态仍按顺序处理；真实 Provider 多调用和 Electron UI 的人工端到端验收仍待补充。

### 6.17 第十六批：Agent 权限范围与审批授权持久化

完成日期：2026-08-22

目标：让审批决定具有明确的生命周期，并把“Agent 一直允许”安全地绑定到当前 Agent 配置，而不是绑定到 Renderer 或全局进程。

改动清单：

- [x] 审批支持 `once`、`session`、`agent` 三种范围；拒绝和取消不产生授权。
- [x] `agent` 规则保存到 `ApplicationSettingsStore.agentDirectory.agents[].permissions.allow`，保留精确匹配和仅末尾 `*` 的命令前缀匹配。
- [x] Main 根据持久化会话绑定解析 Agent，不接受 Renderer 指定 Agent；规则未命中继续审批，当前不维护独立 deny 列表。
- [x] `read_external_file` 在审批前只解析 canonical path、类型和大小；无论 `full_access` 或临时对话都必须逐次审批，不能保存会话或 Agent 长期规则。
- [x] Main 保存 Agent 规则后通过独立 IPC 事件通知 Renderer 刷新配置，避免 Renderer 的旧快照覆盖刚写入的权限。
- [x] Agent 配置页展示和编辑长期 allow 规则；工作区外读取不出现在长期规则编辑器中。

自动验证记录（2026-08-22）：

- Protocol 权限 Schema：41/41 通过，覆盖审批 scope、末尾通配和非法中间通配。
- Desktop 权限/工具定向测试覆盖精确与通配命令、Agent 持久化、会话隔离、外部读取审批前不读正文、路径/文件变化校验和审批恢复幂等。
- Desktop/Web typecheck 通过；完整测试和真实 Electron 审批 UI 仍以本批最终执行结果为准。

当前边界：暂不引入沙箱、Worktree 或 deny 规则；应用级 `unavailable` 仍由 Main 在副作用前直接拒绝。长期权限只影响对应 Agent Profile，不能由 Agent 消息或 Subagent 转授。

### 6.18 第十七批：AGENT_HOME 与 ThreadLog v1 影子日志（进行中）

开始日期：2026-08-27

目标：先建立可迁移的数据根目录和每 Conversation 的顺序日志，不改变现有 SQLite 上下文、查询、UI 或工具执行主链。

改动清单：

- [x] 新增 `AGENT_HOME` 解析：显式值必须是绝对路径；默认 `<os.homedir()>/.agent`。
- [x] 启动时将旧 `userData` 中受管的数据库、设置、附件、Skill、MCP 和 Checkpoint 文件复制到新根目录；不触碰 Chromium Cache / Cookie / Local Storage，也不删除旧数据。
- [x] 定义 `ThreadLog v1` 的 header、UUID eventId、单调 sequence 与尾行截断恢复；为正常 Run 写入用户消息、Run、助手消息、工具请求/结果和压缩 Checkpoint 的关键事件；当 `EventProjector` 可用时，普通首轮和被消费的 `queue` 输入以单个 `run_queued`、运行中的 Pending 新增/编辑/排序/升级以带附件预约的 `pending_messages_updated`、删除以保留取消审计并释放附件的 `pending_message_cancelled`、Steer 在模型安全边界消费时以 `user_message`、执行边界以 `run_started`、工具调用前的 Assistant 回合以 `assistant_message`、Tool 开始/结果以 `tool_call_requested / tool_result`、压缩结果以 `context_checkpoint`、编辑最新用户消息以 `run_replaced`、不涉及 Subagent 跨对话交付的 Run 终态以 `run_terminal` 事件先落 JSONL，再物化对应 SQLite 投影。Subagent 终态继续使用既有跨 Conversation SQLite 原子完成链路。
- [x] 为 Agent Home、ThreadLog 恢复和完成 Run 的写入顺序补充自动测试。
- [x] 增加 `EventProjector` 和 per-Conversation 投影游标：日志事件只幂等写入独立索引表，启动时补投影，并逐条校验 JSONL 与索引的 eventId / sequence / type / 时间 / payload；不覆盖现有业务表。
- [x] 为旧 SQLite-first 会话增加一次性 `legacy_snapshot_imported` 日志导入；会话、Run、模型历史、Timeline 和 Context Checkpoint 可从该快照恢复。
- [x] `ContextCompiler` 在存在 ThreadLog 时从日志重放模型消息与 Checkpoint，并以 SQLite FTS 仅补充相关历史；ThreadLog 尾部使用增量缓存，避免每次模型请求重扫完整 JSONL。
- [x] 新建 Run 冻结稳定排序的 Tool Manifest（名称与内容哈希）；恢复时若可调用工具发生变化则安全失败，旧快照无该字段时保持兼容恢复。
- [x] 跨 Conversation 的协作消息以 `messageId` 去重追加到目标 ThreadLog，并以模型可见的用户消息形式重放；投递重试或启动补投影不会重复污染上下文。
- [x] 新 Run 的初始上下文若已包含未读 Agent 消息，`beforeModel` 只消费其已读/自动回复状态、不再重复追加；运行中才送达的消息仍在下一安全边界注入一次。
- [x] Subagent 创建后的任务包和 queued Run 事件立即追加到其独立 ThreadLog，避免 ThreadLog 上下文路径丢失子任务正文。
- [x] 富 ThreadLog 事件可在 Conversation 元数据仍存在但业务投影缺失时重建核心 Run、Timeline、模型消息、工具结果、Checkpoint、任务清单、排队消息与 Subagent 关系；`run_started` 不会被自动重放，Subagent 完成与 `agent_message_read` 会恢复交付及已读关联；已有 SQLite 业务行不被回放覆盖。
- [x] 非尾部损坏的 JSONL 在启动时改名为 `.corrupt-*` 保留，重置其派生事件索引后由 SQLite 快照生成新日志；运行中读取失败的上下文临时降级到 SQLite，不阻断该次模型请求。
- [x] Task List 支持 `pending`、`running`、`blocked`、`failed`、`completed`；Run 结束不再把 `running` 任务强制改成 `completed`，无任务清单时不预留其最大 Token 容量。
- [ ] 增加完整业务事实的事件回放比对和 JSONL → SQLite 全量业务投影；完成前 SQLite 仍是 UI、Timeline、Run 等业务写入主链，JSONL 不是唯一规范源。

自动验证记录（2026-08-27）：

- `pnpm --filter @agent/desktop typecheck` 通过。
- `pnpm --filter @agent/desktop lint` 通过；同批涉及的 Runtime / ThreadLog / Projector 文件也执行了定向 ESLint。
- `pnpm --filter @agent/desktop test` 通过：50 个测试文件、377 项测试；新增覆盖运行中 Steer 在 JSONL 写入后、SQLite 投影前崩溃时，启动能先恢复 Pending 快照、再把输入消费进活跃 Run；也覆盖从公开 `attachmentRefs` 重建已发送附件元数据和 Pending 附件预约，不把受管绝对路径写进 JSONL。
- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 均通过；测试为 Protocol 5 个文件 / 42 项、Desktop 50 个文件 / 377 项、Web 32 个文件 / 150 项。Web 构建保留既有的大 bundle 提示。
- `git diff --check` 通过；`EventProjector` 定向回归覆盖事件幂等索引、游标推进、普通写前事件追赶、Steer 消费、附件元数据 / Pending 附件恢复与启动补投影。

当前边界：此批保留旧配置文件名以降低迁移风险；不合并 `settings.json`，不迁移临时工作区位置，不改造附件引用格式，不替换既有 Tool Handler 或 ToolRuntime。外部开源工具实现只作为后续逐项适配与评测的参考，不能绕过现有权限、审批与 IPC 合同。

## 7. 每批交付模板

后续每批完成时，在本文件对应复选框打勾，并记录：

```text
批次：
完成日期：
修改文件：
自动测试：
手工验收：
用户结论：通过 / 需修复
遗留风险：
对应提交：
```

不得只写“测试通过”。需要保留命令、通过数量、失败项和未执行原因。

## 8. 暂缓事项

以下能力没有证据表明必须在本轮后端治理中实现：

- 把 Agent Runtime 拆成独立进程或远程服务。
- 为每个 Agent 创建 OS 线程、Worker 或子进程。
- 动态插件市场和热加载工具。
- Java/Python 后端、消息队列或分布式事务。
- 向量数据库和跨项目自动长期记忆。
- 完整 PTY、Git、MCP、受管浏览器 Runtime。

只有出现第二个真实宿主、明确性能瓶颈、崩溃隔离要求或已验证的并发压力后，才重新评估这些设计。

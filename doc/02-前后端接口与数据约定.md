# 前后端接口与数据约定

> 文档状态：开工基线  
> 更新时间：2026-09-02
> 关联文档：[第一阶段需求与验收标准](./01-第一阶段需求与验收标准.md)、[模型接入、MCP、Skill 与 Agent 对话设计](./08-模型接入、MCP、Skill与Agent对话设计.md)

## 1. 目的

本文档定义 `apps/web`、Electron Preload、Agent Runtime、本地工具和存储之间的最小合同。目标是让同一套 React UI 可以运行在浏览器和 Electron 中，同时确保 Renderer 不能绕过权限边界直接访问 Node 能力。

本文档定义的是边界和语义，不要求首个提交一次性实现全部接口。

## 2. 边界原则

- `apps/web` 只依赖 `packages/protocol`，不导入 Node、Electron、SQLite、文件系统或 PTY 模块。
- Electron Preload 实现 Desktop Transport，Web 开发模式实现 Mock Transport。
- UI 只调用 `AgentClient`，不判断 `window.__TAURI__`、`window.electron` 或运行平台后直接调用系统 API。
- 命令使用请求/响应语义；长任务、流式消息和工具过程使用事件流。
- 所有跨边界数据都可序列化，并通过 Zod Schema 校验。
- Schema 以 `packages/protocol` 为唯一来源，Renderer 和 Main 不各写一套类型。
- 纯 Renderer、可安全回退且不参与业务事实的界面偏好可以使用 `localStorage`，无需新增 IPC 或 SQLite 字段。左侧对话树以版本化键保存项目、团队、置顶、临时分组，以及项目、团队和子对话节点的展开状态；无记录或记录损坏时回退默认值，其中“临时”默认收起。该状态只影响可见性，不改变当前对话、项目选择、归档或团队业务事实。

## 3. AgentClient

`apps/web` 只认识以下接口方向：

```ts
export interface AgentClient {
  getCapabilities(): Promise<CapabilitySet>;

  selectWorkspace(): Promise<Workspace | null>;
  listWorkspaceEntries(input: ListWorkspaceEntriesInput): Promise<WorkspaceEntry[]>;
  readDocument(input: ReadDocumentInput): Promise<DocumentContent>;
  saveDocument(input: SaveDocumentInput): Promise<SaveDocumentResult>;

  listSessions(input?: ListSessionsInput): Promise<SessionSummary[]>;
  createSession(input: CreateSessionInput): Promise<Session>;
  loadSession(input: LoadSessionInput): Promise<SessionSnapshot>;
  renameSession(input: RenameSessionInput): Promise<void>;
  deleteSession(input: DeleteSessionInput): Promise<void>;

  forkConversation(input: ForkConversationInput): Promise<ConversationSummary>;
  ensureTeamMemberConversation(input: EnsureTeamMemberConversationInput): Promise<TeamMemberConversationView>;

  chooseConversationAttachments(input: ConversationReferenceInput): Promise<ConversationAttachment[]>;
  readConversationAttachmentPreview(input: ReadConversationAttachmentPreviewInput): Promise<ConversationAttachmentPreview>;
  listDraftConversationAttachments(input: ConversationReferenceInput): Promise<ConversationAttachment[]>;
  removeConversationAttachment(input: RemoveConversationAttachmentInput): Promise<void>;

  startRun(input: StartRunInput): Promise<RunAccepted>;
  cancelRun(input: CancelRunInput): Promise<void>;
  resolveApproval(input: ResolveApprovalInput): Promise<void>;
  writeClipboardText(text: string): Promise<void>;

  listModelProviders(): Promise<ModelProviderView[]>;
  listProtocolAdapters(): Promise<ProtocolAdapterView[]>;
  saveModelProvider(input: SaveModelProviderInput): Promise<void>;
  testModelProvider(input: TestModelProviderInput): Promise<ModelConnectionResult>;
  listModelProfiles(input?: ListModelProfilesInput): Promise<ModelProfileView[]>;
  setAgentModelPolicy(input: SetAgentModelPolicyInput): Promise<void>;

  listMcpServers(): Promise<McpServerView[]>;
  saveMcpServer(input: SaveMcpServerInput): Promise<void>;
  testMcpServer(input: TestMcpServerInput): Promise<McpConnectionResult>;
  setMcpServerEnabled(input: SetMcpServerEnabledInput): Promise<void>;

  listSkills(input?: ListSkillsInput): Promise<SkillView[]>;
  setSkillEnabled(input: SetSkillEnabledInput): Promise<void>;

  listTeamWorkItems(input: ListTeamWorkItemsInput): Promise<TeamWorkItemView[]>;
  getTeamWorkItemExecution(input: GetTeamWorkItemExecutionInput): Promise<TeamWorkItemExecutionView>;
  getTeamCollaborationProjection(input: GetTeamCollaborationProjectionInput): Promise<TeamCollaborationProjection>;
  submitTeamWorkItem(input: SubmitTeamWorkItemInput): Promise<TeamWorkItemView>;
  updateTeamWorkItem(input: UpdateTeamWorkItemInput): Promise<TeamWorkItemView>;
  deleteTeamWorkItem(input: DeleteTeamWorkItemInput): Promise<void>;
  updateTeamWorkItemPermission(input: UpdateTeamWorkItemPermissionInput): Promise<TeamWorkItemView>;
  publishTeamWorkItem(input: PublishTeamWorkItemInput): Promise<TeamWorkItemView>;
  addTeamWorkItemComment(input: AddTeamWorkItemCommentInput): Promise<TeamWorkItemView>;
  requestTeamWorkItemRework(input: RequestTeamWorkItemReworkInput): Promise<TeamWorkItemView>;
  acceptTeamWorkItem(input: AcceptTeamWorkItemInput): Promise<TeamWorkItemView>;

  listPlugins(): Promise<PluginCatalogEntry[]>;
  setPluginEnabled(input: SetPluginEnabledInput): Promise<PluginCatalogEntry>;

  subscribe(listener: (event: ClientEvent) => void): () => void;
}
```

接口按实现环境分为：

| 实现 | 使用位置 | 能力 |
| --- | --- | --- |
| `DesktopAgentClient` | Electron Renderer | 通过 Preload IPC 调用完整本机 Runtime |
| `MockAgentClient` | Vite 浏览器开发 | 固定夹具和模拟事件，不访问本机系统 |
| `WebAgentClient` | 后续完整 Web 版 | 通过 HTTP/WebSocket 调用远程 Agent Server |

### 3.1 模型供应商品牌标识

`SaveModelConfigurationInput.providerIcon?: ModelProviderIcon` 与 `ModelProfile.providerIcon?: ModelProviderIcon` 用于供应商展示，不参与模型请求或凭据认证。Desktop 凭据配置以 Provider 维度持久化该字段，缺失的历史配置等价于 `auto`；Renderer 在 `auto` 时从供应商名称生成 1 至 2 个字符，其他值使用本地打包的品牌图标或中转站字母标识。可选值由 `modelProviderIconSchema` 统一校验，避免 Renderer 与 Main 各自维护不兼容的名称集合。

第一阶段实现前两个。`WebAgentClient` 只保留接口位置，不写空壳功能和假成功返回。

### 3.2 Plugin Catalog IPC

Desktop Preload 使用 `plugin.list` 和 `plugin.set_enabled` 两个窄 IPC。`PluginCatalogEntry` 只暴露 `id`、名称、版本、内容哈希、启用状态和更新时间；受管包的绝对目录、manifest 原文与任何本机路径均不跨 Renderer 边界。启停只更新 Catalog 状态，影响后续新建 Run 的 Plugin 快照，不中断或重写运行中的 Run。〔FACT｜`packages/protocol/src/plugin.ts`；`apps/desktop/src/main/ipc/register-main-ipc.ts`〕

## 4. CapabilitySet

UI 不能根据平台字符串猜测能力，应读取 Runtime 返回的能力集合：

```ts
export type CapabilitySet = {
  mode: "desktop" | "web" | "mock";
  workspace: boolean;
  fileWrite: boolean;
  process: boolean;
  pty: boolean;
  git: boolean;
  managedBrowser: boolean;
  mcp: boolean;
  skills: boolean;
  docxConversion: boolean;
};
```

不可用能力在 UI 中不显示操作入口，或以真实禁用状态呈现；不能点击后才返回“尚未实现”。

Desktop 当前将 `git`、`pty`、`managedBrowser` 标记为 `true`，它们分别表示用户可在右侧工作区打开 Git 工作区、项目交互终端和隔离浏览器。该能力集合本身不授予模型任意 Git、浏览器或 PTY 控制；模型的持久终端和浏览器访问只通过下述项目对话受限工具合同开放，Vite Mock 的三个字段继续为 `false`。

### 4.1 开发工具工作区合同

- `git_review.get_snapshot` 接收 `projectId`，先解析包含该项目目录的 Git 仓库根目录，再返回当前分支、本地分支列表、上游、ahead/behind、文件状态、暂存状态和逐文件增删行统计；所有文件路径均相对仓库根目录。`git_review.get_file_diff` 默认只读取用户展开文件的 3 行上下文并返回最多 2 MB 的文本 Diff；IPC 对 `contextLines` 的安全上限仍为 10,000，当前 Renderer 在“已省略未修改内容”处单次最多请求 120 行，其余超长区段继续折叠，项目即使登记在仓库子目录也不能重复拼接路径。Renderer 在当前应用生命周期内按项目共享 Git 快照、同一上下文等级的逐文件 Diff 和在途请求；项目激活时即低优先级加载快照，并按最多 2 个并发预取前 20 个文本 Diff，用户展开同一路径时复用缓存或在途请求，二进制文件不预取。缓存最多保留 4 个最近项目、30 分钟未访问项目和约 32 MB Diff 文本，超限后按 LRU 回收并优先保留当前项目；快照超过 30 秒后在项目重新激活或窗口回焦时后台复核。AI 文件工具完成时，Renderer 只失效事件指定的项目和路径，250 ms 内合并快照刷新并优先重新预取这些路径；显式刷新和成功 Git 操作仍使该项目全部 Diff 失效。Git 审阅栏宽度达到 760px 时树与选中文件 Diff 双栏展示，较窄时仍在文件行下方展示；两种布局都以 hunk 间的省略带表达远处未修改内容。短时间切换标签、对话或页面不得重复请求；应用重启、缓存回收或失效后重新读取。
- `git_review.run_operation` 使用判别联合限制为逐文件/全部暂存与取消暂存、按已选路径批量暂存、按已选路径取消跟踪、创建并切换本地分支、切换已有本地分支、按已选路径提交、`pull --ff-only` 和普通推送。Renderer 的复选框只保存待操作选择，不直接写入 Git；未跟踪文件组的加号会跟踪并暂存已选路径，已跟踪更改组的减号会只从 Git 索引移除已选路径、保留其本地文件；提交会先暂存并仅提交这些唯一的相对路径，保留其他已暂存但未选的文件。创建分支可选传入一个已经由 Main 校验存在的本地分支作为 `startPoint`；未传时仍从当前 HEAD 创建。Renderer 不能传入任意 Git 参数；首次推送只允许为当前分支设置 `origin` 上游。该工作区不提供丢弃修改、删除分支、硬重置或强制推送。
- `terminal.get_configuration/save_configuration` 持久化侧边终端的默认 Shell、每种 Shell 可选的可执行文件绝对路径、字体、字号和行高，以及一次性命令结果的输出解码字符集。默认字体栈优先使用支持 Nerd 图标的等宽字体并回退至 `Segoe UI Emoji`。侧边终端的 Shell、路径和字体设置不改变模型 `run_command` 使用的受控非交互 Shell；只有输出字符集继续用于一次性命令 stdout/stderr 解码。`terminal.session_open/write/resize/close` 管理以项目根目录为工作目录的 PTY。新终端按保存的默认 Shell 启动，优先使用该 Shell 配置的路径；未配置 PWSH 路径时 Main 尝试常见 Windows 安装目录，再回退到 `pwsh.exe`。交互 PTY 必须覆盖继承的 `TERM` 为 `xterm-256color`、提供 `COLORTERM=truecolor`，并清除继承的 `NO_COLOR`，不能把桌面开发进程的 `TERM=dumb` 或禁色设置传给用户 Shell。已经打开的会话不因设置变化而重启；Main 持有进程对象，Renderer 只接收 `terminal.session_event` 的有界输出或退出事件。关闭终端标签必须结束对应 PTY，应用退出和主窗口关闭会统一释放遗留会话。
- `workspace_terminal.open_requested` 是 Main 到 Renderer 的一次性终端标签请求，携带 `requestId`、当前 `conversationId`、已解析的 `projectId`、可空 `requestedName` 和已经由 Main 创建的 PTY 会话。Renderer 只在对应的当前项目会话中挂载这个同一会话，并依据所有已打开的文件、聊天和工具标签计算唯一名称。随后 Renderer 调用 `workspace_terminal.opened`，只回传同一 `requestId` 和严格受限的 `resolvedName`。Main 在收到确认前不完成模型 `create_terminal`/兼容 `open_terminal` 工具调用，因此模型结果会同时得到可持续读写的 `terminalId`、原请求名、实际标签名和是否调整；`execute_terminal_command` 向该 PTY 写入一条命令，`read_terminal_output` 用输出游标读取有界增量。`create_terminal`、`open_terminal` 和 `execute_terminal_command` 先生成无副作用的待批准动作，复用 `command-run` 应用策略、Agent/会话允许规则和 LangGraph interrupt 审批；批准前不得创建 PTY、创建标签或写入命令。批准后实际创建/写入进入项目命令操作协调边界；命令发送及即时输出采样结束后释放协调记录，后台长驻 PTY 不长期占用项目写锁。会话只归属创建它的项目对话；窗口不可用、会话已切换或十秒内未确认时工具失败并关闭已创建 PTY。
- `workspace_browser.open_requested/opened/close_requested` 将模型创建的隔离浏览器会话与 Renderer 侧边标签绑定：Main 先创建受管浏览器，再等待 Renderer 返回去重后的标签名；模型关闭浏览器时 Main 通知 Renderer 同步移除该标签。首方 `agent.browser` 工具组只允许项目对话操作自己创建的会话，采用“观察有界页面文本和元素引用，再以引用执行受控动作”的合同，不接受任意 JavaScript；浏览器会话仍沿用隔离 Session、无凭据 URL并拒绝权限申请。模型发起的打开、导航、点击、填写、选择、按键、滚动和关闭使用独立 `browser-control` 应用策略，默认 `ask`，并在批准前只解析参数和校验会话归属；`browser_observe` 和 `browser_wait` 只返回有界只读状态，不产生审批。浏览器工具不提供读取本地下载文件内容的接口。
- `browser.get_configuration/save_configuration/clear_data` 持久化默认搜索引擎、新浏览器标签的默认缩放比例和下载前询问设置，并清除隔离浏览数据；`managed_browser.open/navigate/command/capture/set_bounds/close` 管理 Electron `WebContentsView`。Renderer 提交地址栏文本、受限浏览器命令、菜单锚点、应用当前深浅色和视图边界；`set_bounds` 使用经具名 Preload 入口发送的单向 IPC。普通窗口变化按动画帧合并最新边界；面板拖动期间由分隔条根据实时宽度直接推导原生 View 边界，每次移动都同步真实 Chromium 页面，松手后再以 DOM 最终边界校准。Main 将域名、IP、`localhost` 和显式 HTTP/HTTPS 输入解析为导航地址，将其他文本交给用户选择的搜索引擎，并通过 `managed_browser.event` 回传标题、URL、加载、历史与当前缩放状态。新标签默认显示应用自有空白起始页。Main 在页面加载前向 Chromium 设置对应的首选颜色和自动深色策略；应用主题变化时同步浏览器背景、网页渲染和已打开覆盖菜单，已有 HTTP/HTTPS 页面重新加载一次以应用 Chromium 的按页配色。`capture` 只按需返回当前可见视口的有界 JPEG 快照，不改变原生 View 的可见性，也不进入持久化存储。缩放、打印、截图、开发者工具、下载记录和清除隔离浏览数据由 Main 执行；`Ctrl + 鼠标滚轮` 的缩放请求同样由原生 `WebContents` 处理并回传状态。浏览器使用隔离 Session，禁用 Node、权限申请和携带 URL 凭据的导航；下载默认进入系统下载目录，也可按设置逐次询问保存位置；关闭标签必须销毁对应 View。
- 当前活动的对话或工具页不回收；顶部标题栏中仍打开的主对话标签也是明确的缓存保护项，不受普通页面的数量和时限淘汰。关闭主对话标签只解除该页面的缓存保护，不删除 Conversation；关闭当前标签后 Renderer 激活相邻标签，关闭最后一个标签后清空当前主对话选择。除这些保护项外，普通非活动页缓存最多保留 8 个、最长 1 小时。短时间切换时，终端 PTY、浏览器 `WebContentsView`、Git 快照和 Diff 都必须保留；超过数量或时限后，Renderer 卸载非活动页面，主动关闭对应 PTY 或浏览器 View，并在用户再次打开时重新请求 Git、重新启动终端、或从上次 URL 重新加载浏览器。浏览器三点菜单和下载面板由 Main 创建独立、透明、跟随应用主题的覆盖 `WebContentsView` 显示在实时网页上方；打开和关闭覆盖层不得隐藏、截图替换、重载或重建网页 View。

## 5. 命令与事件

### 5.1 命令

命令由 UI 发起并等待一次响应，例如创建会话、开始 Run、取消和审批。每个跨进程命令包含：

```ts
type CommandEnvelope<T> = {
  schemaVersion: 1;
  requestId: string;
  command: string;
  payload: T;
};
```

- `requestId` 用于关联响应和避免重复提交。
- 有副作用命令不得因 IPC 重连自动重试。
- Runtime 在执行前再次校验 Schema 和权限，不能只相信 Renderer 已校验。

### 5.2 事件

Agent Runtime 向 UI 推送统一事件：

```ts
type EventEnvelope<TType extends string, TPayload> = {
  schemaVersion: 1;
  eventId: string;
  sessionId: string;
  runId?: string;
  sequence: number;
  timestamp: string;
  type: TType;
  payload: TPayload;
};
```

- `sequence` 在单个 Session 内严格递增，用于恢复后补齐事件。
- UI 按 `eventId` 去重，按 `sequence` 排序。
- 实时事件先推送 UI，持久事件成功写入后才能成为恢复事实。
- UI 重新订阅时携带最后确认的 `sequence`，Runtime 返回缺失事件。
- `conversation.updated` 的 `ConversationSummary.activeRunId` 仅表示当前可取消的模型 Run；`activeSubagentCount` 由 `subagent_tasks` 中该父对话直属的 `queued`/`running` 任务实时聚合。UI 在任一字段表示活动时显示“运行中”，但停止按钮只能取消 `activeRunId` 指向的 Run。
- `ConversationSummary.avatarIcon` 是可选的受控 Agent 图标 ID。Subagent 只在请求提供合法枚举值或绑定 Agent Profile 含预设图标时写入 `conversations.avatar_icon`；非法的可选 `icon` 按未提供处理，不能阻断 Subagent 创建。字段为 `null` 时，Renderer 根据子 Conversation ID 即时生成稳定的 5×5 对称 Identicon，不写图片或新增持久字段。同一 ID 的图案和色调保持一致；Renderer 仍不能执行模型提供的 SVG、URL 或组件名。
- `model.retry_updated` 携带完整 `model_retry` Timeline 项，表示一次语义模型请求正在退避重试、已经恢复或最终失败。同一请求的多次更新复用相同 Timeline ID；一旦产生可见文本或工具调用，不再重放模型请求。

### 5.3 第一阶段事件类型

```text
session.created
session.renamed
run.started
run.status_changed
message.created
message.delta
message.completed
tool.requested
approval.requested
approval.resolved
tool.started
tool.output
tool.completed
tool.failed
summary.created
artifact.created
agent.model_selected
agent.model_changed
mcp.connection_changed
mcp.capabilities_changed
skill.enabled
skill.disabled
run.completed
run.failed
run.cancelled
run.interrupted
```

`message.delta` 和 `tool.output` 是高频事件。UI 可以逐块显示，但存储层按时间或大小合并写入，不能每个 Token 或每个字符单独提交事务。

### 5.4 右侧栏项目文件编辑 IPC

右侧栏用户编辑使用与项目工具相同的项目操作锁，但不经过 Agent 权限审批。Preload/Renderer 暴露两个窄接口：

```ts
writeProjectFile({
  projectId: string,
  path: string,                 // 项目根相对 POSIX 路径
  content: string,
  expectedContent: string | null,
}): Promise<ProjectFile>;

readProjectPreviewImage({
  projectId: string,
  sourcePath: string,            // Markdown 文件路径
  path: string,                  // 正文中的原始图片引用
}): Promise<{ mimeType: string; data: string }>;
```

- `writeProjectFile` 在 `ProjectToolRegistry` 内执行，参与文件/命令统一锁；命令或其他项目变更占用时返回 `CONFLICT`（底层 `PROJECT_OPERATION_CONFLICT`）。
- 写入前必须将磁盘当前文本与 `expectedContent` 比较；不一致返回 `FILE_CHANGED`，不得覆盖。成功后返回新的 `ProjectFile`，Renderer 更新下一次保存的预期内容。
- `readProjectPreviewImage` 以 `sourcePath` 所在目录解析相对引用，再执行真实路径边界、符号链接、扩展名和体积校验；远程/data/blob 引用不经此通道，图片以 Base64 data URL 返回。右侧文件工作区直接预览图片时复用该通道，支持 PNG/APNG、JPEG/JFIF、GIF、WebP、BMP、SVG、ICO 和 AVIF，保持 8 MB 上限且只读。
- `ProjectFile.truncated === true` 表示仅返回前 2 MB，UI 必须保持只读，禁止把截断内容写回原文件。
- `fileChange` 事件刷新无脏草稿标签；有脏草稿时只标记外部变更，交由下一次 `expectedContent` 校验处理。

## 6. Run 状态机

```text
queued
  -> running
      -> waiting_approval
          -> running
          -> blocked
      -> completed
      -> failed
      -> cancelled
      -> interrupted
```

约束：

- 一个 Session 第一阶段最多有一个活动 Run。
- `completed`、`failed`、`cancelled`、`blocked` 和 `interrupted` 是终态。
- 应用异常退出后，原 `running` 和 `waiting_approval` Run 在下次启动时转为 `interrupted`。
- `interrupted` Run 可以由用户发起新的继续请求，但不能从中间自动重放有副作用的工具。
- 状态转换通过存储事务提交，并生成对应事件。
- Run 开始时写入不可变 `modelSnapshot`；Agent 后续修改模型策略不反向改变历史 Run。
- 模型切换只影响下一次模型调用；需要立即切换时先取消当前 Run，再创建新 Run。

## 7. 消息模型

### 7.1 消息复制导出

复制最终 Assistant 回复时，Renderer 按该消息的 `runId` 从 Timeline 选出同一 Run 的 Assistant 回复和工具记录，并按 Timeline 顺序生成 Markdown。命令包含命令行、状态、退出码、stdout 和 stderr；文件工具包含 Diff；读取、搜索和其他工具输出为有界结构化文本。用户消息、隐藏 reasoning、内部工具 JSON、错误实例 ID 和受管绝对路径不进入导出。没有工具记录时仍复制原始 Assistant Markdown。代码块围栏会根据内容中的反引号动态加长，避免破坏 Markdown。

桌面端使用 `clipboard.write_text` IPC 调用 Electron Main 原生剪贴板。请求通过 `clipboardWriteTextIpcArgumentsSchema` 校验，文本上限为 2,000,000 个字符，成功返回 `void`；浏览器 Mock 使用浏览器 Clipboard API。

```ts
type Message = {
  id: string;
  sessionId: string;
  runId?: string;
  role: "user" | "assistant" | "system";
  status: "streaming" | "completed" | "failed" | "cancelled";
  createdAt: string;
  completedAt?: string;
  durationMs?: number;
  blocks: ContentBlock[];
};

type ContentBlock =
  | { type: "text"; text: string }
  | { type: "tool_call"; toolCallId: string }
  | { type: "tool_result"; toolCallId: string }
  | { type: "approval"; approvalId: string }
  | { type: "artifact"; artifactId: string; label: string }
  | { type: "summary"; summaryId: string };
```

Reasoning 不按模型名称判断，而按 Adapter 事件语义分流。供应商明确返回的 `kind=content` 是可展示的完整思考内容，并以可选 `reasoningContent` 写入 `conversation_timeline`；Renderer 再按 `runId` 将同一 Run 的完整思考、Assistant 过程说明和工具记录依照 Timeline 顺序投影到一个“工作过程”折叠区。该投影只改变显示结构，不重排或删除持久记录，也不改变后续模型上下文；工具结果返回后产生的新思考继续进入同一过程区，最终 Assistant 回答仍独立显示在其下方。完整思考不拼入 Assistant 正文，也不进入复制结果或 `model_messages.content`。普通 `assistant.delta` 正文若以成对 `<think>` 或 `<thinking>` 标签开头，按兼容接口泄漏的摘要处理而不升级为完整 Reasoning：Renderer 在完成后的历史投影中移除整个标签段，只保留标签后的正常正文，并保持原始 Timeline 不变。`kind=summary` 只作为当前模型请求的实时思考摘要显示；`kind=progress` 表示模型主动给用户看的短进度更新，不等同于思维链。两者都在输出正文、开始工具或 Run 结束后清除，不要求写入 Timeline。未通过供应商接口返回的模型私有推理仍不采集、不展示。

Renderer 显示消息时间时按用户本地日历格式化：当天只显示时分，前一天显示“昨天 + 时分”，同一年较早日期显示“月日 + 时分”，跨年显示完整年月日和时分。Assistant 最终消息的 `completedAt` 与 `durationMs` 是 Timeline 查询根据所属终态 Run 的 `updated_at - created_at` 派生出的只读展示字段；不会重复写入消息正文、模型上下文或 ThreadLog。

模型可见正文与 Reasoning 流必须分开：OpenAI Responses、Anthropic Thinking 和 Gemini Thought Summary 作为 `summary` 事件；OpenAI Chat 兼容端点中明确返回的 `reasoning_content` 默认作为 `content` 事件，但 GPT-5.6 系列通过该兼容端点返回的同名无类型字段按供应商摘要语义归为 `summary`，不得生成持久 `reasoningContent`。供应商明确标注为面向用户的短进度时使用 `progress`。`assistant.reasoning_delta` 携带所属 `messageId`、`modelId`、`kind` 和 `reset`，Renderer 只把 `content` 合并到对应 Assistant Timeline 消息，`summary/progress` 只更新当前请求的临时状态；Renderer 同时过滤历史版本误存的 GPT-5.6 摘要。三类事件均不能重复拼入 Assistant 正文。不可读的推理签名、加密内容和工具调用签名只保存在 Provider State 中，不生成可见 Reasoning 事件；Provider State 只在 `apiFormat + baseUrl + modelId` 与当前请求完全匹配时重放。新 LangChain 快照使用版本 2，迁移期继续只读兼容 AI SDK 版本 1 的 `assistantMessage/openAiChatReasoningContent`，新写入不再产生旧格式。

取消 Run 时，已收到的 Assistant 流式文本以 `status=cancelled` 的消息保留；没有收到文本则不创建空的 Assistant Timeline。该消息同时进入模型历史，便于用户继续对话时保留已经看到的内容。

### 7.2 编辑最后一条已发送用户消息

Renderer 通过 `conversation.replace_latest_message` 提交 `ReplaceLatestConversationMessageInput`。该命令只接受非空文本和当前对话最后一条已发送用户消息的 ID，返回新 Run 的 `RunAccepted`。

边界约束：

- Main 必须重新校验消息归属和“最后一条用户消息”条件，不能信任 Renderer 是否正确隐藏编辑按钮。
- 正在运行的原 Run 必须先取消并完成资源清理；替换事务不能与模型、命令或文件 I/O 混在同一个 SQLite 事务中。
- SQLite 事务复用原 Timeline 消息 ID 和附件绑定，更新消息正文、Run ID 与时间，删除旧 Run 在该消息之后的 Timeline/Model Message，并使覆盖该消息的旧 Context Checkpoint 失效。
- 新 Run 记录、用户 Timeline 和模型消息必须在同一事务内建立；事务失败时保留旧消息与旧回答。
- 旧 Run 记录不删除，外部工具副作用不回滚。替换成功后 Renderer 以返回的用户消息更新 Timeline，并通过正常 Run 事件继续接收流式回答。
- 用户未显式选择新 `@对话/@文件` 引用时保留原消息的隐藏引用；显式选择时以新引用集合构造模型输入。

### 7.3 从 Assistant 最终回复创建分支

Renderer 通过 `conversation.fork` 提交 `ForkConversationInput`：

```ts
type ForkConversationInput = {
  conversationId: string;
  throughMessageId?: string;
};
```

- 不传 `throughMessageId` 时，供右侧菜单创建普通侧边聊天；该对话保留 `parentConversationId` 并只在右侧打开，不进入左侧对话树。父对话没有 Context Checkpoint 时复制全部模型消息；存在 Checkpoint 时复制该摘要、作为覆盖边界锚点的最后一条消息，以及边界之后未覆盖的消息。锚点只用于映射新对话 Checkpoint，不会再次发送给模型。
- Runtime 创建 `kind = "subagent"` 的子对话时只保留 `parentConversationId` 作为生命周期关系，不复制父对话的 `model_messages`、Context Checkpoint、Timeline 或附件引用。子对话的第一条模型可见用户输入必须是 `spawn_subagent.task`；需要的父对话事实由调用方显式写入该任务。
- 传入 `throughMessageId` 时，该 ID 必须属于源对话中某个 Run 的最终、已完成 Assistant Timeline 消息；Main 将它映射到对应 `model_messages.sequence`，把该序号及之前的模型消息物化复制到相同项目或临时分组中的同级普通对话。新对话 `parentConversationId = null`，不与源对话建立继承关系。
- 同级分叉标题按源对话当前标题递增为 `源标题 (1)`、`源标题 (2)`；编号在同一项目或临时分组中取已有同名前缀的最大值加一。
- 指定边界后的用户消息、Assistant 消息和工具调用/结果不能进入分支。边界内模型消息引用的附件复制元数据并重映射 ID，其他附件不复制。
- 只有覆盖序号不晚于分支边界的 Context Checkpoint 才能复制到新对话；覆盖到边界之后的摘要不得注入历史分支。
- 同级回复分叉复制截至边界的 `conversation_timeline`，包括用户消息、Assistant 回复、工具过程和 Agent 消息；所有 Timeline ID、Run ID、会话 ID 和附件 ID 必须重映射，使复制后的历史完全属于新对话。源对话和新对话后续独立写入。
- 右侧侧边聊天 Timeline 仍为空，只展示创建侧边聊天之后的新消息。
- Renderer 关闭右侧侧边聊天标签时，使用既有 `deleteConversation({ conversationId })` 删除该子对话；仅在删除成功后移除标签。删除失败时保留标签并在当前工作区显示错误，不能把它降级为“隐藏后可重新打开”的状态。
- Renderer 将普通侧边聊天的 `activeRunId`、运行终态和 `hasUnreadResult` 聚合到左侧父对话状态：任一侧边聊天运行时父项显示运行中；未确认的成功结果显示蓝色圆点，未确认的失败结果显示红色圆点。完成事件不能因父对话或侧边标签当前可见而自动确认；用户点击父对话，或在主对话、任一侧边聊天中点击、输入、发送或执行用户滚动时，Renderer 通过既有 `markConversationResultViewed` 逐项确认这一组普通对话。模型输出造成的自动滚动不能被视为用户确认。

## 8. 工具合同

### 8.1 ToolDefinition

```ts
type ToolDefinition = {
  name: string;
  description: string;
  inputSchema: JsonSchema;
  permission: "read" | "write" | "execute" | "network";
  timeoutMs: number;
  cancellable: boolean;
  resultPolicy: "inline" | "truncate" | "artifact";
};
```

### 8.2 ToolCall

```ts
type ToolCall = {
  id: string;
  sessionId: string;
  runId: string;
  name: string;
  arguments: unknown;
  workspaceId: string;
  status: "requested" | "waiting_approval" | "running" | "completed" | "failed" | "cancelled";
};
```

`workspaceId` 由 Runtime 根据 `sessionId` 解析，不能由模型指定。项目会话解析为项目根目录；临时对话可以没有 `workspaceId`，这不代表没有工具能力：`run_command` 使用按会话隔离的系统临时目录，`web_search` 和 Agent/任务/附件工具不依赖工作区。只有用户通过系统目录选择器显式附加目录后，临时对话才获得文件工具所需的工作区；文件工具参数统一使用相对于该根目录的 POSIX 路径。

### 8.3 ToolResult

```ts
type ToolResult = {
  toolCallId: string;
  status: "completed" | "failed" | "cancelled";
  summary: string;
  data?: unknown;
  artifactIds: string[];
  truncated: boolean;
  error?: AgentError;
};
```

- `summary` 是提供给模型和 UI 的有限结果。
- 完整长输出写入 Artifact，并明确 `truncated: true`。
- Tool Result 不能直接包含无法序列化的 Error、Stream、Buffer 或进程对象。
- `conversationToolItem.executionMode`（`parallel`/`serial`）记录 Runtime 对该调用作出的调度决定；字段可选以兼容历史 Timeline 行，不能被 Renderer 反向修改。
- `web_search` 是无需工作区的公共网络只读工具，使用内置免费 DuckDuckGo Provider；结果只包含有界的标题、摘要、域名和 URL，不提供网页交互能力，并受网络可用性和上游限流影响。
- `read_external_file` 只接受绝对路径，主进程在审批前只解析 canonical path、文件类型和大小，不读取正文；该工具即使在 `full_access` 下也必须逐次经过审批，临时对话同样可用。
- 同一项目中的 Agent 文件变更和 `run_command` 进入同一冲突协调边界；读目录、读文件、搜索和文件查找可由同一模型轮并发执行。模型一轮最多返回 32 个、且可以混合不同类型的 `toolCalls`；超过上限时整批在任何工具启动前以不可自动重试的 `MODEL_RUN_LIMIT_REACHED` 失败。Runtime 按 Tool Handler 的调度策略分组，只读并发窗口最多 8 个调用，非 `read_only` 模式下独立命令默认并行、每个窗口最多 4 个进程，`parallel=false` 可显式降级为串行；`ask_before_changes` 仍逐条审批，但已批准的独立命令可以重叠执行；结果按 `toolCallId` 关联，不按完成顺序改变消息顺序。文件变更在首个应用前保存每个调用的 `expectedContent`；后到的旧 Diff 在应用时返回 `FILE_CHANGED` 或项目冲突并作废，不允许覆盖先完成的变更，也不得自动重放。多个不同文件的审批/写入仍是逐文件的非原子批次。

## 9. 审批合同

```ts
type ApprovalRequest = {
  id: string;
  sessionId: string;
  runId: string;
  toolCallId: string;
  kind: "outside_workspace_read" | "file_write" | "apply_patch" | "command" | "browser";
  title: string;
  details: ApprovalDetails;
  createdAt: string;
};

type ApprovalResolution = {
  approvalId: string;
  decision: "approved" | "rejected";
  scope: "once" | "session" | "agent";
  resolvedAt: string;
};
```

`scope=once` 只放行当前 Tool Call；`scope=session` 写入 Main 进程内按 conversation 维度的会话授权，应用重启后消失；`scope=agent` 写入当前会话绑定 Agent 的 `permissions.allow`。命令允许规则支持精确命令和末尾通配（例如 `mvn package *`），通配只按 token 边界匹配，不匹配 `mvnx`；文件规则按工具和项目相对路径匹配。持久终端创建/命令复用 `run_command` 规则类型，浏览器副作用使用 `browser_control` 规则类型；浏览器规则只保存动作及目标引用或 URL，不把填写文本持久化进允许规则。未命中允许规则的副作用继续审批，当前不维护独立 deny 列表。历史应用设置缺少 `browser-control` 时按 `ask` 读取。

Subagent 继承父 Run 的权限模式，不继承父 conversation 的 `once` 或 `session` 授权。Renderer 通过子会话的活动 Run 和 `awaiting_approval` Tool 恢复父对话中的待审批投影；父、子入口都只提交既有的 `approved + scope + runId + toolId`，Main 仍按 Tool 所属 conversation 解析权限主体。过期或已处理的 Tool 审批返回 `APPROVAL_EXPIRED`，不得映射为 `FILE_CHANGED`。

工作区外文件读取是跨边界敏感操作，只支持 `once`，不能保存为会话或 Agent 永久允许；`read_only` 不会扩大写入权限，应用策略 `unavailable` 在 Main 端直接拒绝而不弹审批。

用户在编辑器中亲自点击保存属于直接用户操作，不走 Agent 审批；由模型发起的同一文件写入必须审批。两类操作在事件中使用不同 `actor` 标识。

## 10. 统一错误

```ts
type AgentError = {
  id: string;
  code: AgentErrorCode;
  message: string;
  retryable: boolean;
  details?: Record<string, unknown>;
};
```

第一阶段错误码至少包含：

```text
VALIDATION_FAILED
CAPABILITY_UNAVAILABLE
WORKSPACE_REQUIRED
PATH_OUTSIDE_WORKSPACE
FILE_CHANGED
APPROVAL_REJECTED
APPROVAL_EXPIRED
PROCESS_TIMEOUT
PROCESS_CANCELLED
PROCESS_FAILED
MODEL_AUTH_FAILED
MODEL_CONFIGURATION_INVALID
MODEL_RATE_LIMITED
MODEL_TIMEOUT
MODEL_PROVIDER_UNAVAILABLE
MODEL_RESPONSE_INVALID
MODEL_RUN_LIMIT_REACHED
NETWORK_UNAVAILABLE
STORAGE_FAILED
ARTIFACT_NOT_FOUND
PERMISSION_DENIED
CONFLICT
IPC_FAILED
INTERNAL_ERROR
```

错误返回用户可读 `message`，日志可以记录内部堆栈，但不能将 API Key、完整环境变量或敏感文件内容放入 `details`。

- `id` 是每次错误实例的 UUID，只用于关联 IPC、Run、工具结果和主进程日志；不拼接到用户可见错误文案。
- 主进程 IPC Handler 统一经过错误边界。错误通过可序列化 Envelope 传输，由 Preload 恢复为 `AgentClientError`；Renderer 不直接展示 Electron 原始异常。
- `run.finished` 保留兼容用的展示字符串，并通过 `agentError` 携带结构化错误；工具失败结果同样包含 `agentError`。
- 模型接口失败时，`details.status` 保存 HTTP 状态，`details.providerMessage` 只保存脱敏、截断后的供应商错误摘要；网络或内部异常可通过 `details.technicalMessage` 显示安全技术摘要。UI 显示这些有限详情，不展示完整响应体、HTML 网关页面或内部堆栈。
- API Key、Bearer Token 和可识别的 `sk-` 密钥在持久化、IPC、工具结果和日志前脱敏。
- 错误按归属渲染：进入自动重试的模型请求由 `model_retry` Timeline 卡片持续展示，工具错误留在对应工具行内，未进入重试的模型请求错误仍作为 Assistant 时间线错误，普通界面操作错误留在当前工作区操作区域。Assistant 错误卡默认只显示分类标题、用户可理解的结论和下一步建议；脱敏后的 HTTP 状态、供应商摘要、网络/内部技术详情放在“查看技术详情”中按需展开，完整响应体、框架排障链接和内部堆栈不得默认暴露。工具调用的原始参数和长结果仍由工具卡片自身控制展开。
- `model_retry` 保存 `attempt/maxAttempts/retryInMs/status/reason`。恢复时原卡片更新为 `completed`；耗尽时更新为 `failed` 并保留最终脱敏 Provider 错误，不再追加“模型响应无法处理”之类的通用重复失败消息。应用重启把仍在执行的 Run 保守标记失败时，同一事务也把该 Run 下遗留的 `retrying` 记录更新为 `failed`、清空 `retryInMs` 并保留原错误，Renderer 不得继续显示倒计时或旋转状态。
- Renderer 渲染异常由 React Error Boundary 接管；主进程记录未捕获异常、未处理 Promise、Preload 失败和 Renderer 退出。Renderer 异常退出只自动恢复一次，避免重载循环。

## 11. SQLite 数据约定

第一阶段 SQLite 是主数据源，至少包含：

```text
schema_migrations
workspaces
teams
projects
team_projects
work_items
tasks
task_dependencies
agent_profiles
agent_instances
agent_threads
delegations
agent_messages
task_leases
sessions
runs
messages
content_blocks
events
tool_calls
approvals
summaries
artifacts
model_providers
provider_endpoints
protocol_adapters
model_profiles
agent_model_policies
run_model_snapshots
conversation_attachments
conversation_deletion_tasks
mcp_servers
mcp_capabilities
skills
agent_skills
run_skill_snapshots
```

关键约束：

- 所有 ID 使用应用生成的 UUID。
- 时间统一存 UTC 毫秒或 ISO 字符串，数据库内保持一种格式。
- `events(session_id, sequence)` 建立唯一索引。
- `runs`、`messages`、`tool_calls` 保存当前可查询状态，`events` 保存追加式过程。
- 当前状态与对应事件在同一事务中提交。
- `model_profiles` 只保存非敏感配置和密钥引用，不保存 API Key。
- `model_providers` 保存 Adapter 类型、端点和凭据引用；同名模型通过 `provider_id + model_id` 唯一识别。
- 当前模型配置文件同时保存全局默认模型、最近一次用户手动选择，以及每个模型的 `connectionStatus`、`connectionStatusUpdatedAt`、`lastSuccessfulAt`。它是配置与健康事实源；Renderer/Zustand 不复制第二份可写目录。
- `conversations` 保存当前 `provider_id + model_id + reasoning` 选择快照。新普通对话由 Main 按“最近用户选择 -> 默认模型”解析；侧边对话和 Subagent 创建时复制父 Conversation，之后可独立更新。
- Renderer 切换 Conversation、切换侧边标签或折叠/展开右侧工作区时，必须从目标 Conversation 的选择快照恢复控件；全局最近选择和默认模型只参与新 Conversation 初始化或缺失快照的兼容兜底，不能覆盖已有快照。
- Renderer 可保活当前 Conversation、顶部仍打开的所有主对话标签，以及普通缓存层中一小时内访问过的最多 8 个页面。标题栏标签是缓存保护的可见契约：点击左侧对话会新增或激活唯一标签，运行中的对话标签显示动态状态，关闭标签后该页面重新服从普通缓存淘汰；标签集合只属于当前 Renderer 生命周期，不是 Conversation 持久化事实。该缓存只持有展示实例和只读模型状态快照；归档、删除、项目移除或超时淘汰后重新通过 `AgentClient` 从持久化事实加载，不能把缓存写成第二份业务事实源。多个保活页面共享一个底层 `conversation.run_event` 订阅，再按 `conversationId` 分发。
- `conversation.set_model_selection` 只接受已配置的 Provider、模型和已启用思考选项。普通/侧边 Conversation 的用户更新同时刷新最近选择；`subagent` 和受管 Team WorkItem 执行树 Conversation 都不刷新该全局偏好。受管 Team Conversation 的模型/思考更新必须在同一事务中写入当前 Conversation 展示快照和其 WorkItem 的后续 Run 策略；当前 Run 的 `run_model_snapshots` 不可修改。它仍拒绝改项目，且正在运行时的用户输入只能作为同一 Run 的 `steer`。
- `run_model_snapshots` 保存实际模型、选择来源和能力快照，创建后不可修改。
- MCP、Skill 配置按应用、Team、Project 或 Agent 范围启用；它们不能扩张 Task 的项目权限。
- Artifact 大内容不保存为 SQLite BLOB。
- 每个 `tasks.project_id` 必填；跨项目 WorkItem 通过多个 Task 表达，不允许一个 Task 绑定多个项目。
- `task_dependencies(task_id, depends_on_task_id)` 建立唯一约束并禁止自依赖。
- 一个 Task 同时最多有一个未过期的 `task_lease`，领取和续租使用事务。
- Team Lead、Agent Thread 和任务调度事件进入同一事件体系，运行状态不能只保存在内存 Store；这里的 Agent Thread 是逻辑对话归属，不对应 OS 线程或进程。

### 11.1 Artifact

```ts
type ArtifactRecord = {
  id: string;
  sessionId: string;
  runId?: string;
  kind: "terminal_log" | "diff" | "browser_snapshot" | "screenshot" | "tool_output";
  relativePath: string;
  mimeType: string;
  byteSize: number;
  sha256: string;
  createdAt: string;
};
```

Artifact 必须位于应用数据目录，数据库只保存相对路径。读取时重新解析绝对路径并确认仍位于 Artifact 根目录，防止路径注入。

### 11.2 ConversationAttachment

```ts
type ConversationAttachment = {
  id: string;
  conversationId: string;
  messageId: string | null;
  source: "project" | "upload";
  kind: "file" | "image";
  name: string;
  mimeType: string;
  sizeBytes: number;
  projectPath: string | null;
  contextTokens: number;
  truncated: boolean;
  createdAt: string;
};
```

- 用户可以通过输入框的附件按钮多选文件，也可以把系统剪贴板中的文件或截图直接粘贴到输入框。文件选择由 Main 原生对话框返回路径；粘贴内容通过有 25 MB 上限的 `conversation.import_attachment_bytes` IPC 传递 Base64 字节，Renderer 不提交本地路径。两种入口都立即复制到 `userData/conversation-files/<conversationId>/` 并创建 `messageId = null` 的持久草稿。发送消息时，创建 Run、用户消息、模型消息和附件绑定在同一 SQLite 事务内提交。
- SQLite 的 `conversation_attachments` 保存公开元数据、受管文件路径和可选提取文本路径；`model_messages.attachment_ids_json` 保存有序附件 ID。Renderer 永远不能获得受管绝对路径。需要展示图片时，只能使用 `conversation.read_attachment_preview` 携带当前 `conversationId + attachmentId` 读取已校验为图片的有界 Base64；Main 必须验证附件属于该会话，普通文件不能通过这个接口读取正文。
- 项目内文件和项目外上传文件都保存不可变快照。`source` 和 `projectPath` 只描述快照来源，不改变模型处理方式；项目中未显式添加的文件继续通过工作区工具读取实时内容。
- 文本、常见代码、PDF 和 DOCX 在 Main 进程提取为 UTF-8 文本。超过 48,000 字符时只注入头 36,000 和尾 12,000 字符，全文由只读工具 `read_attachment(attachment_id, offset, limit)` 分段读取。
- PNG、JPEG、GIF 和 WebP 不写入 SQLite 或模型消息 JSON；只在实际构造模型请求时读取快照并编码。OpenAI Chat Completions、OpenAI Responses、Anthropic Messages 和 Google Gemini Adapter 分别转换为各自的多模态内容块。若 Provider 明确拒绝图片能力，且当前最新用户消息没有新图片、首个请求也尚未产生任何输出，Adapter 只把较早历史图片降级为包含附件名称、ID 和来源的文字说明并重试一次；当前消息新附图片时不降级，继续返回真实的不支持错误。
- 每条消息最多 10 个附件，单个附件最多 25 MB。发送前草稿可移除；编辑最新已发送用户消息时，可以保留或移除原附件，也可以选择、粘贴新附件。`conversation.replace_latest_message` 接收编辑后的最终有序 `attachmentIds`，只允许引用该消息原附件或本会话当前未绑定草稿；新草稿与替换 Run 在同一 SQLite 事务或同一个 write-ahead `run_replaced` 事实中绑定。被移出消息的旧快照不再进入 Timeline 和模型上下文，但继续作为受管不可变快照保留到会话清理，避免数据库与文件系统之间产生不可恢复的半删除。
- Pending Message 被删除时，其附件不回退成输入框草稿。取消记录继续持久保存并暂时保留附件绑定，作为可重试的文件清理事实；Main 删除无其他附件引用的受管文件后才移除附件行，文件被占用或进程中断时在下次启动继续。旧版本已经解绑为草稿的取消附件按取消消息原始 `attachmentIds` 识别并清理；已经进入模型上下文或绑定正式消息的附件不参与该修复。
- 输入框附件保持单行、等高、横向溢出；已发送消息的正文气泡在上，图片预览或文件卡片以独立附件条显示在正文下方，不嵌入文字气泡。编辑最新用户消息时，编辑器以相同附件条展示当前最终选择，并允许逐项移除或继续添加。
- 创建分叉时形成不可变上下文快照：右侧侧边入口在无 Checkpoint 时复制全部 `model_messages`，有 Checkpoint 时只复制覆盖边界锚点和未覆盖消息并映射摘要；回复分叉入口只复制指定最终 Assistant 回复及之前的消息。仅当最新 `conversation_context_checkpoints` 未覆盖到回复分叉边界之后时才复制摘要，并把 `covered_through_sequence` 映射到新对话消息序号。
- 回复分叉使用普通 `agent` 身份、`parentConversationId = null` 并复制源对话当时的 Agent 配置；右侧侧边聊天使用普通 `agent` 身份和源对话 `parentConversationId`；任务 Subagent 使用 `subagent` 身份并必须有对应任务生命周期记录。
- 回复 Fork 将边界内的可见 Timeline 写入同级新对话，Renderer 立即显示分叉前历史；右侧侧边聊天仍不复制 Timeline。源对话后续消息不自动同步。分叉只复制快照内模型消息实际引用的附件元数据，并同时重映射附件 ID、附件所属消息 ID 和 Timeline ID；底层不可变文件快照可以共享。
- 清理文件前必须查询 SQLite 引用；删除父对话、子对话或归档到期都不能删掉仍被其他对话引用的快照。
- 删除会话时先在一个 SQLite 事务中创建 `conversation_deletion_tasks`，保存根会话、整棵会话树、受管文件路径、状态、重试次数和最后错误，同时把相关会话标记为 `deletion_pending`。待删会话不再出现在列表、Agent 发现或临时工作区恢复中。
- `conversation_deletion_tasks` 不使用指向 `conversations` 的级联外键，确保进程退出、文件占用或数据库收尾失败时恢复事实仍然存在。任务可以处于 `pending`、`running` 或 `failed`，启动和每日归档清理都会重试未完成任务。
- 文件清理只删除任务记录的受管路径。判断共享引用时忽略待删会话，但保留任何正常会话仍引用的快照；`ENOENT` 按已完成处理。会话树对应的 ThreadLog JSONL 同样在任务内删除，缺失按已完成处理；文件完成后，删除会话树和任务记录在同一个 SQLite 事务内提交。
- 会话删除任务在文件清理成功后还会删除对应 Run 的 LangGraph Checkpoint thread；Checkpoint 数据库暂时独立于业务库，删除失败会让原清理任务保持 `failed` 并在启动/定时任务中重试，不能只删除业务会话行。
- 手动删除和归档 30 天到期删除必须调用同一个会话删除 Use Case，不能直接执行 `DELETE FROM conversations` 后再补删文件。

## 12. 模型上下文合同

`ContextBuilder` 的输出由以下部分组成：

```ts
type ModelContext = {
  systemInstructions: string;
  workspaceContext: WorkspaceContext;
  conversationSummary?: StructuredSummary;
  recentMessages: ModelMessage[];
  relevantArtifacts: ArtifactExcerpt[];
  availableTools: ModelToolDefinition[];
  estimatedInputTokens: number;
  estimatedAttachmentTokens: number;
  inputBudget: number;
};
```

`getConversationContextUsage` 除总输入、压缩阈值和输出预留外，还返回 Skill 目录、活动任务清单和 Skill 正文预留的独立 Token 估算。Renderer 用这些字段区分“实际输入”和“预留容量”；Skill 正文预留不能计入已使用输入，工具 Schema 仍属于实际发送的系统能力上下文。响应中的可选 `providerCache` 来自 Provider 实际用量而非本地估算，并只统计当前所选 `apiFormat + baseUrl + modelId`，切换 Provider 或模型后不混用不同缓存池：`latest` 给出最近一次请求的输入、模型输出、缓存读取、缓存写入、命中率及相对上一次已上报请求的百分点变化，`cumulative` 只对明确返回缓存读取字段的请求按 `sum(cachedInputTokens) / sum(inputTokens)` 计算本对话 Token 加权平均命中率，并同时返回已上报数和总请求数。未返回缓存字段与真实 `0%` 必须区分。当前 MCP Runtime 尚未实现，因此不存在可发送的 MCP Tool Schema。

- Skill 目录摘要作为系统上下文的一部分发送；模型调用 `load_skill` 后，`SKILL.md` 正文由 Runtime 临时注入下一轮模型请求，不新增聊天 Timeline/用户消息。Graph State 只保存 Skill 的 `id/version/contentHash`，恢复或压缩后重新读取正文并校验哈希；内容变化时 Run 失败并要求重新加载。Agent 为 `custom` 能力范围时，其 `skillIds` 同时约束目录、加载、reference 和恢复注入；`inherit_all` 才继承全部满足作用域的已启用 Skill。
- `read_skill_reference` 的 `path` 必须是已激活 Skill 下 `references/` 或 `templates/` 的相对路径。Main 端拒绝绝对路径、`..`、符号链接、NUL、非法 UTF-8 和超大文件，并在读取前执行字节上限检查；Skill 文档不能扩大权限或绕过审批。`team` 作用域只在带 `teamId` 的团队会话生效。
- LangGraph Checkpoint 的 `thread_id` 使用应用生成的 Run ID。`checkpoint_ns` 由 LangGraph 为 Middleware、ToolNode 和嵌套图组合生成，允许最长 8,192 字符，不能套用普通标识符的 512 字符上限；它仍只作为 SQLite 预编译语句参数使用。应用重启只自动恢复尚未进入模型/工具节点的 `queued` Run；已经处于 `running` 的 Run 按可能发生外部副作用处理为失败，不自动重放。恢复不会创建重复用户消息。

- 当前 Run、未完成审批和明确用户约束不可被摘要替代。
- 当前 active 任务清单是独立的临时运行上下文块。它在每次模型请求前刷新，并以不落盘的 `user` 消息追加到稳定历史和当前 Run 消息之后，避免任务状态变化使 Provider 的整段历史前缀缓存失效；它不进入 Timeline，也不写回 LangGraph 消息。每项包含 `title`、`status` 和可选 `reason`；`reason` 仅用于 `blocked`、`failed` 的简短、可展示解释，最长 600 字符。已有不含 `reason` 的持久化清单读取为 `null`，以保持兼容。
- Tool Schema、输出预留、安全余量和 Skill 正文保留预算先从模型窗口中扣除。启用 Skill Runtime 时，ContextManager 预留 `max(1024, min(12000, floor(effectiveContextWindow * 0.12)))` Token；没有模型窗口时按 48,000 Token 计算。Skill 正文实际注入和用量估算使用同一预算，正文仍不进入 Timeline。
- 模型窗口、最大输出和输出预留来自本次 Run 的 Model Profile 快照；用户修改配置只影响后续模型调用。
- 旧工具输出先裁剪或 Artifact 化，再摘要对话。
- 文本附件按实际注入的标题和预览估算 Token；图片按文件大小在 1,024 至 8,192 Token 之间保守估算并计入图片说明。Provider 返回精确用量后可用于校准，但不能依赖所有厂商都返回图片 Token。
- 压缩顺序是先裁剪旧工具输出，再合并较早完整轮次的隐藏摘要，最近两轮尽量原样保留。轮次摘要使用动态门槛：只有一种有效来源时单项软上限约 448 tokens；用户/Assistant/工具来源越多，单项门槛逐级收紧，最低约 320 tokens，同时整轮总量不得超过当前单项门槛的两倍。小幅位于边界内的内容仍直接本地封装，不额外调用模型；越过单项或整轮门槛才生成一次短模型摘要。本地封装的单字段最多保留 720 个归一化字符。摘要用来源标签区分普通用户、Steer、其他 Agent、Assistant 与工具。进入摘要的附件保留附件 ID、名称、类型、来源、项目相对路径、读取状态和截断状态，不把图片 Base64 放进摘要。
- 自动达到阈值和用户发送 `/compact` 都进入同一增量压缩流程。`/compact` 可在未达到阈值时主动压缩符合条件的较早完整轮次；压缩运行状态以 Timeline-only 的 `compact_context` Runtime Activity 记录，不生成模型可见的 ToolMessage。Renderer 将它显示为带压缩图标的居中说明分割线，运行中显示流光和实时耗时，完成后显示“已压缩”和最终耗时；实际处理条数仅在悬停提示中提供。
- 发送前计算本轮最低预算。如果本轮内容自身仍超过压缩阈值，不创建 Run、不绑定草稿附件，并向对话区返回可操作错误；用户可减少附件或调整模型上下文/压缩阈值后重试。
- 每轮隐藏摘要写入 `conversation_turn_summaries` 并记录 `run_id` 与覆盖终点；它是可由原始记录再生成的 SQLite 加速数据。正式压缩结果写入 `conversation_context_checkpoints`，覆盖序号只能前进。
- `conversation_turn_summaries` 必须由独立的前向迁移创建，不能只追加到已经执行过的初始建库脚本；现有版本 19 数据库启动时通过版本 20 补建该表和覆盖边界索引，避免真实用户库因缺表导致摘要生成和压缩失败。
- 摘要失败不能删除原始消息；自动压缩最多缩小一次完整旧前缀重试，手动强制压缩可继续逐级缩小，但始终保留当前任务、压缩期间新到输入和最近保护轮次。失败后的强制重试入口只显示图标，操作含义和真实失败信息通过悬停提示提供。用户停止手动压缩时，已落盘 Checkpoint 保持有效，活动显示为“已暂停”；期间的 Queue、Steer 和 Agent 输入继续等待，不因 Run 结束而自动放行。用户点击继续压缩图标或再次发送 `/compact` 后从最新覆盖边界续做，压缩完成才按 Steer 优先、Queue 其次的既有顺序交给模型。

## 13. 密钥与配置

- `protocol_adapters` 表示已安装的对话协议实现；`provider_endpoints` 保存用户选择的协议 Adapter、Base URL、可选请求路径和凭据引用。
- `model_profiles` 保存模型 ID、别名、角色、能力、上下文窗口、输入/输出上限和配置来源。
- API Key 由 Electron Main 使用 `safeStorage` 加密后写入独立凭据文件。
- Preload 不提供读取明文 Key 的接口，只提供保存、删除和连接测试命令。
- Renderer 提交新 Key 后，Main 完成加密并立即清理可控内存引用。
- 日志序列化器对 `authorization`、`apiKey`、`token` 和常见密钥格式做统一脱敏。
- MCP Token、敏感环境变量和远程授权凭据使用同一凭据引用机制，不写入 Server JSON 或 SQLite。
- Agent 的模型继承、固定、自动选择和用户动态修改规则以[模型接入、MCP、Skill 与 Agent 对话设计](./08-模型接入、MCP、Skill与Agent对话设计.md)为准。
- Agent 管理数据根目录由 Main 解析：优先绝对路径环境变量 `AGENT_HOME`，否则为 `<os.homedir()>/.agent`；首次迁移仅复制既有 `userData` 下的 Agent 数据，不移动或删除旧数据。显式配置 `AGENT_HOME` 时，Electron 的 `userData` 固定为 `<AGENT_HOME>/electron-profile`，首次创建该目录会复制既有 `Local State`，使 `safeStorage` 的加密上下文与凭据文件一起保持稳定；重复启动不得覆盖已经存在的 `Local State`。Renderer 不接收管理目录绝对路径，也不能直接读取凭据、数据库或 JSONL。
- 当前 `ThreadLog v1` 位于 `<AGENT_HOME>/conversations/<conversationId>.jsonl`。仅创建普通空 Conversation 时不创建文件；首次模型可见输入、Subagent 的首个任务输入，或有模型消息/Run/Timeline/Checkpoint 的旧 Conversation 迁移时才创建。空侧边分支为了恢复父子关系仍保留极小关系日志；侧边聊天的 JSONL 属于临时会话数据，关闭标签触发可恢复删除任务并一并移除。已迁移的普通 Run、Queue / Steer、工具、Checkpoint 和终态会先追加 JSONL，再由 `EventProjector` 幂等物化 SQLite；SQLite 仍承担列表、Timeline、Run、FTS 和跨对话关系查询。`ContextCompiler` 在存在且可读取的日志时从其重放模型消息与 Checkpoint；若运行中遇到不可读日志，当前请求安全降级到 SQLite 历史，启动时会把原文件改名为 `.corrupt-*` 保留、清空其派生投影游标，并由 SQLite 快照重建新的 JSONL。Renderer 不能直接扫描 JSONL，仍只能经过 IPC 查询 SQLite 投影。附件引用只记录公开元数据，由 Main 的 `AttachmentStore` 解析受管路径，绝对路径和二进制不会进入 IPC 或日志。`EventProjector` 投影合同独立版本化。

## 14. 版本与兼容

- 所有持久事件和跨进程 Envelope 包含 `schemaVersion`。
- SQLite 使用只向前执行的版本化 Migration。
- Preload API 暴露 `apiVersion`，Web UI 启动时检查兼容性。
- 不在第一阶段支持任意旧版数据库；开发期结构变更通过 Migration 或明确重建测试数据库。
- 会话导出格式与内部数据库 Schema 分开版本化，避免数据库调整破坏导入导出。

## 15. 团队与任务调度合同

完整行为见[多 Agent 团队与任务调度设计](./05-多Agent团队与任务调度设计.md)。协议层至少定义以下核心对象：

```ts
type WorkItemStatus =
  | "inbox"
  | "triaging"
  | "needs_clarification"
  | "planned"
  | "queued"
  | "running"
  | "waiting_user"
  | "blocked"
  | "reviewing"
  | "completed"
  | "failed"
  | "cancelled";

type TaskStatus =
  | "pending"
  | "ready"
  | "assigned"
  | "running"
  | "waiting_approval"
  | "blocked"
  | "reviewing"
  | "completed"
  | "failed"
  | "cancelled"
  | "interrupted";

type TaskView = {
  taskId: string;
  workItemId: string;
  projectId: string;
  title: string;
  objective: string;
  acceptanceCriteria: string[];
  dependencyTaskIds: string[];
  priority: number;
  status: TaskStatus;
  assignedAgentId?: string;
  expectedOutput: "finding" | "patch" | "review" | "test_result" | "artifact";
};

type TaskListView = {
  workItemId: string;
  revision: number;
  currentTaskId?: string;
  tasks: TaskView[];
};

type AgentInstanceKind = "team_lead" | "standing" | "ephemeral";

type AgentInstanceStatus =
  | "starting"
  | "idle"
  | "working"
  | "waiting"
  | "stopping"
  | "stopped"
  | "interrupted"
  | "failed";
```

第一阶段命令至少包括：

```text
team.create
team.get
project.register
project.list
work_item.submit
work_item.set_priority
work_item.cancel
task.list
task.set_priority
task.request_replan
task.cancel
agent.list
agent.send_message
```

调度器内部命令不直接暴露给 Renderer。`agent.spawn`、`task.create`、`task.revise`、`task.assign`、`task.claim`、`task.complete` 和租约续期只由经过验证的 Team Runtime 调用，UI 通过事件观察结果，不能伪造 Agent 或任务状态。

第一阶段新增事件至少包括：

```text
work_item.received
work_item.status_changed
task.created
task.plan_created
task.plan_revised
task.dependency_added
task.assigned
task.status_changed
agent.spawn_requested
agent.spawned
agent.status_changed
agent.message_sent
agent.result_returned
agent.stopped
routing.decided
capacity.limit_reached
```

关键约束：

- 所有团队事件包含 `teamId`；任务事件同时包含 `workItemId`、`taskId` 和 `projectId`。
- `TaskListView` 是 `tasks`、依赖和事件的查询投影，不单独维护另一份 Todo 状态。
- AI 创建或修改任务列表时递增 `revision` 并保存理由；已完成 Task 重新打开必须产生显式事件并保留原完成证据。
- 创建 Task 不触发 Agent 创建；是否委派由 TeamSupervisor 独立决策。
- `routing.decided` 保存候选集合、过滤原因、选中 Agent 和简短分配理由。
- Agent 间消息的 `actor` 不能是 `user`，也不能携带用户审批凭据。
- Agent 结果通过有界回执和来源 Conversation/Artifact 引用回传，不复制整个执行对话；持久 Agent 以 Markdown `---` 分隔回执和详细结果，自动回执只取分隔线前最多 1000 字符。临时 Subagent 的复杂最终回答同样以分隔线区分最多 2000 字符的完成回执和子对话内的支持细节，简单短答直接回传；完整回答由调用方按 `maxTokens` 主动读取，已知核验目标可附带最长 500 字符的 `query` 进行相关消息检索。
- 同一 Team 的事件需要团队级单调序号；单 Session 事件序号继续保留，用于局部恢复和渲染。
- 自动扩容达到并发、实例、时间或 Token 上限时发送事件并排队，不通过重试绕过限制。

当前第一条生产竖切已经实现 `team.instance.list/create/rename/reorder/set_archived/delete/ensure_member_conversation`、`team.work_item.list/get_execution/submit/update/delete/update_permission/publish/add_comment/request_rework/accept`，以及模型可见的 `submit_team_work_item`、`get_team_work_item_status`。团队目录里的 Team 是创建模板；全局、项目和对话 TeamInstance 必须由用户在对应导航层级显式创建。模型工具只接受当前主 Conversation 可见的 `teamInstanceId`：全局实例任意项目可见，项目实例只对同项目可见，对话实例只限所属来源 Conversation 可见。工具不能接收模板 `teamId`、实例名或 `executionScope` 来静默派生实例。`submit` 持久化 `teamInstanceId` 和 `sourceConversationId`，Main 复核实例作用域和来源项目；`get_team_work_item_status` 仅查询当前 `sourceConversationId` 自己提交的 WorkItem，省略 ID 最多列 20 项，指定 ID 返回截断且有界的任务、参与者、阻塞和结果状态，不包含完整需求、事件、协作边或画布坐标。团队页也可由用户选择一个已有的全局/项目 TeamInstance 和已注册项目后直接创建并发布 WorkItem，项目实例会锁定自己的项目，全局实例才允许跨项目选择。该入口省略来源 Conversation，但仍复用同一 `submit` 契约、实例与项目校验、模型策略和调度链路，绝不静默创建实例。每个实例保留自己的 Team Lead/成员持久 Conversation，每个 WorkItem 独立审计、验收并在该实例上串行调度。模型/工具循环仍由现有 AgentRuntime 执行，Team Lead 只可用持久 `send_agent_message` / `wait_for_agent_message` 协作，不能为团队成员调用 `spawn_subagent`。暂停模板对应实例允许提交但不启动 Run；应用设置恢复启用后重新检查队列。`update` 对所有未删除 WorkItem 保存标题与需求并追加审计事件；当 WorkItem 正在运行时，Runtime 同时把完整新版本作为 `steer` 交给 Team Lead，空闲的执行根会启动受 WorkItem 约束的协调 Run，当前 Run 已发生的副作用不会回滚。`delete` 先把 WorkItem 标记为软删除并从默认列表隐藏；正在执行时还会取消执行树中的活动 Run，Conversation、Run、事件和协作记录继续保留。`update_permission` 可用于任何未终态 WorkItem。`add_comment` 把用户评论追加为 `commented` WorkItem 事件，不写入 Agent Conversation，也不创建 Run；只有显式 `request_rework` 才复用原执行 Conversation 创建新 Run。`publish` 是用户显式操作：`queued` WorkItem 立即重新检查调度，`blocked/failed/cancelled` WorkItem 清除旧执行占用并以新修订重新排队，但保留原 Conversation、Run 和事件历史；运行中、待验收和已完成 WorkItem 拒绝重复发布。根 Run 结束后，只有持久成员没有排队或运行中的 Run，且 Team Lead 已消费已送达的成员结果，WorkItem 才进入 `waiting_user`；否则保持 `running` 并等待自动汇总 Run。自动汇总始终复用提交时冻结的模型、推理强度和权限模式；配置不能恢复时显式阻塞，终态 WorkItem 不能被迟到消息重新启动。待验收或已完成 WorkItem 都可由用户显式返工；返工清除当前完成态投影、增加修订号并保留此前验收、评论、Conversation、Run 和事件历史。`accept` 必须覆盖全部验收条件。

`team.work_item.get_execution` 是按一个 WorkItem 读取的只读执行谱系接口：根节点为该 WorkItem 绑定的持久 Team Lead Conversation，成员来自 `team_member_conversations` 关系；每一次由持久 Agent 消息触发的成员任务保存在 `team_work_item_member_assignments`。返回每个实际参与者的 Conversation 快照、持久化 Agent 身份快照、最近任务分派状态和深度。普通侧边对话虽然也可能有 Conversation 父子关系，但没有 Team 成员关系或分派事实，绝不能被列作团队执行成员；该接口不创建第二份成员状态或缓存。根 Conversation 会在首个 Run 入队前绑定到 WorkItem；应用重启时，进行中的 WorkItem 会阻塞，绝不自动重放文件、命令或网络副作用。

`team.collaboration.get_projection` 是协作图的唯一 Renderer 读取入口，接收 `{ workItemId }` 并返回当前活动计划、参与者、路线和聚合计数。计划由 Team Lead 的 `set_team_collaboration_plan` 发布为追加修订；真实消息继续先写入 `conversation_agent_messages`，提交后再关联当前 WorkItem。没有计划边不影响 `send_agent_message` 投递，Main 把这种合法偏差投影为 `ad_hoc`。投影的 `isLive` 只由自身 WorkItem 是否仍处于 `running` 或 `reviewing` 决定；路线动画必须同时满足 `isLive` 和发送方节点运行中，不能被同一持久成员 Conversation 的后续 WorkItem 带动。节点额外携带受控 `avatarIcon`，以及本 WorkItem 内最近 Assistant 输出的最多 280 字符尾部摘录和对应 `latestOutputRunId`；摘录按 `(workItemId, conversationId)` 持久化快照。实时 `assistant.delta` 仅在事件的 `teamWorkItemId` 与图的 `workItemId` 相同时合并，结束后继续读取该快照，绝不从同一持久成员 Conversation 的后续 WorkItem 借用输出。投影不含 Agent 间消息正文、完整 Assistant 消息、系统 Prompt、工具参数或绝对路径。看板、来源主对话、“任务与验收”和“执行规划”必须读取同一 DTO，不在 Renderer 各自拼装第二份关系状态。〔FACT｜`packages/protocol/src/team-collaboration.ts`；`apps/desktop/src/main/storage/agent-database.ts`；`apps/desktop/src/main/ipc/register-main-ipc.ts`〕

**团队页与对话树的 Renderer 合同**：团队页只显示 WorkItem 的需求、阶段、任务、验收/返工动作，以及实际成员和团队状态；不得在中间区域嵌入成员的 Timeline、工具卡或第二份聊天页面。全局实例和项目实例统一显示在顶层“团队”，项目实例可按项目偏好增加项目目录快捷入口；对话实例直接显示在来源对话展开后的协作树中。全局/项目实例展示配置成员，对话实例只展示已实际参与成员。打开任一成员必须展示其原生持久 Conversation，并保留输入框、待发送消息、停止和审批操作。`conversation.send_message` 收到受管执行谱系 Conversation 时，Main 不会走普通新 Run 路径，而是经 `TeamWorkItemRuntime` 验证当前 WorkItem/Run 后以 `deliveryMode = steer` 投递。Run 终止时 Main 将所有遗留 `awaiting_approval` Tool 行持久化为 `cancelled`，Renderer 不显示审批按钮。〔FACT｜`apps/desktop/src/main/teams/team-work-item-runtime.ts`；`apps/desktop/src/main/agent/agent-runtime.ts`；`apps/desktop/src/main/ipc/register-main-ipc.ts`；`apps/web/src/features/projects/project-navigator.tsx`；`apps/web/src/features/workspace/right-sidebar-workspace.tsx`〕

左侧导航展示的是 `TeamInstance`，不是配置模板，也不再有“团队执行实例”汇总层。顶层“团队”统一创建和展示 global/project 实例；project 实例附带关联项目的当前名称。团队标题行只负责展开/收起和自定义顺序拖拽，不映射或选中 Team Lead；成员行才打开自己的 Conversation。`team.instance.reorder` 必须提交当前未归档 global/project 实例的完整、无重复 ID 顺序并持久化。`ProjectSummary.showTeamsInNavigator` 是默认关闭的持久导航偏好，开启时才在对应项目对话列表中额外展示同一 project 实例。普通对话悬浮操作创建 conversation 实例并在该对话展开树中显示。默认名取模板名，同一可见命名空间重名时追加 `(1)`、`(2)`。实例支持重命名、归档和软删除，删除保留既有 Conversation/WorkItem 审计记录。成员点击调用 `team.instance.ensure_member_conversation` 并复用该实例原生 Conversation，不创建 WorkItem、Run 或 Timeline 副本。

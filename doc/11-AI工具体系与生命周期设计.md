# AI 工具体系与生命周期设计

> 更新时间：2026-08-30
> 状态：内置工具、持久侧边 PTY、首方浏览器工具插件、LangChain `createAgent`/LangGraph ToolNode、批量 Tool Call 调度、interrupt 审批恢复和 Skill 渐进加载已接入；MCP Runtime 仍未实现

## 1. 设计目标

工具层负责把模型意图转换为可授权、可观察、可取消和可审计的本机操作。它不是对 Shell 的替代，也不应为了“结构化”把成熟命令行能力拆成大量薄包装。

采用以下边界：

- 常用、结果需要有界、需要专用 UI 的操作使用结构化工具。
- 复杂搜索、Git、构建、测试和项目自带 CLI 继续使用 `run_command`。
- 普通非交互命令默认使用 `run_command`；只有用户明确要求可见/右侧/交互终端，或任务确实需要可持续接管的 PTY 时，才创建持久侧边终端。
- 文件写入必须生成 Diff，并经过当前对话权限策略。
- 长时间操作返回稳定 ID，等待超时只结束本次等待，不中止后台工作。
- 不同领域保留明确的等待工具，不合并成语义模糊的万能 `wait`。

## 2. 当前内置工具清单

Agent Runtime 的基础工具注册表包含现有项目、Agent、Subagent、任务和附件工具；启用 Skill Runtime 时另外注册 `load_skill` 与 `read_skill_reference`。Skill 工具不执行脚本，不改变项目权限。

| 类别 | 工具 | 结论 |
| --- | --- | --- |
| 即时只读 | `list_directory`、`read_file`、`search_text`、`find_files`、`read_attachment` | 保留。分别覆盖目录、文件片段、文本搜索、文件搜索和对话附件全文分段读取；除附件外需要当前工作区 |
| 受控外部读取 | `read_external_file` | 保留。只接受绝对 UTF-8 文本文件路径；审批前不读取正文，即使 `full_access` 也必须逐次审批，临时对话可用 |
| 公共网络只读 | `web_search` | 已实现。项目和临时对话均可调用；使用免费 DuckDuckGo Provider，返回有界来源摘要和 URL，不执行网页点击或页面交互 |
| 文件变更 | `write_file`、`delete_file`、`replace_in_file`、`apply_patch` | 保留。创建/全量替换、删除、精确替换和单文件多位置补丁语义不同 |
| 命令任务 | `run_command`、`list_background_commands`、`wait_for_commands`、`stop_command` | 保留。默认 `batch` 覆盖短命令和耗时构建/测试，持续流式输出并等待真实退出；仅明确长期存活的服务使用 `service`，登记服务名与稳定 ID 后转后台，可查询、等待或停止 |
| 持久侧边终端 | `terminal_control` | 仅项目对话可用，以 `create/list/write/read/close` 管理模型创建的可见 PTY；命令写入不阻塞，输出按游标有界读取，不取代一次性 `run_command` |
| 首方浏览器插件 | `browser_control` + `browser-use` Skill | `agent.browser` 只向新模型请求暴露一个统一动作工具；按需加载的 Skill 提供操作顺序、安全约束与参数说明。优先使用有界元素引用，并以模型可见截图和真实指针事件覆盖 Canvas、图标、悬停与拖拽；不提供任意页面脚本执行、凭据、权限或下载读取能力；历史 `browser_*` 名称仅保留执行兼容，不再进入工具清单 |
| 项目冲突协调 | `list_project_operations`、`wait_for_project_operation` | 保留，但只应在冲突后使用，不要求模型每次先查询 |
| Agent 通信 | `list_agent_conversations`、`read_agent_conversation`、`send_agent_message`、`wait_for_agent_message`、`set_team_collaboration_plan` | 保留。查找目标、按预算读取或关键词命中、持久发送和有界等待职责不同；最后一个工具只允许当前受管 WorkItem 的 Team Lead 发布完整、有向、可修订的软约束通信计划 |
| Subagent | `spawn_subagent`、`list_subagents`、`wait_for_subagents` | 保留。一次性创建、状态查询和依赖等待形成完整生命周期；创建参数把 `name`、受控 `icon` 与任务正文分开 |
| Team WorkItem | `submit_team_work_item`、`get_team_work_item_status` | 仅在未归档的项目主 Conversation 中注册。提交必须来自用户显式 `@团队` 或明确交办；状态查询只读取当前来源自己提交的 WorkItem，并且只在用户询问或后续工作依赖结果时调用，不能持续轮询或把 Renderer 协作画布写入模型上下文 |
| 任务清单 | `create_task_list`、`update_task_list`、`close_task_list` | 保留。只管理当前对话的任务元数据，不授予文件或命令权限 |
| Skill 上下文 | `load_skill`、`read_skill_reference` | 可选注册。先加载 `SKILL.md` 并保存版本/哈希快照，再按相对路径读取 `references/` 或 `templates/` 下的有界文本；正文进入模型上下文，不写入聊天 Timeline |

历史 UI 中仍兼容 `get_project_info` 的旧记录，但后端不再向模型注册该工具。项目名称和根目录已经动态写入系统上下文，模型无需主动查询当前目录。

## 2.1 多 Tool Call 调度矩阵

模型一轮可以混合返回多个独立 `toolCalls`，但最多 32 个。超过上限时 Runtime 将本轮模型响应归类为 `MODEL_RESPONSE_INVALID`，整批工具均不启动，不能截断后执行前 32 个。这个批量请求不代表所有调用都可以同时执行：Runtime 先由每个 Tool Handler 声明调度策略，再把连续的同类安全调用按有界窗口交给 LangGraph `ToolNode`；结果始终按原始 `toolCallId` 回填，不能按完成先后拼接。

每条 Timeline 工具记录会保留 Runtime 的 `executionMode`（`parallel` 或 `serial`），用于前端显示实际调度结果；它不是模型参数，也不代表同批所有工具都能并行。

`toolCallId` 必须在本轮内唯一，也不能复用当前模型上下文中已经出现过的 ID。`createAgent`/ToolNode 用该 ID 关联完成结果；重复 ID 会让新调用被误判为旧调用。Runtime 在任何新工具启动前拒绝同轮重复；跨轮复用则拒绝该轮新响应，保留此前已经完成的工具事实，并统一映射为 `MODEL_RESPONSE_INVALID`。未知工具不终止整个 Run，而是持久化失败工具行并返回对应失败 `ToolMessage`，让模型下一轮修正工具名。

| 调度组 | 当前工具 | 执行规则 |
| --- | --- | --- |
| 并行只读 | `list_directory`、`read_file`、`search_text`、`find_files`、`read_attachment`、`view_attachments`、`web_search`、`list_project_operations`、`list_background_commands`、`list_agent_conversations`、`read_agent_conversation`、`list_subagents`、`get_team_work_item_status`、已激活 Skill 的 `read_skill_reference` | 同一连续组按最多 8 个调用一个窗口交给 `ToolNode` 并行执行；超出部分进入下一窗口，结果独立关联，取消传播到每个调用；`view_attachments` 每次最多回看四个历史附件 |
| 并行命令 | `run_command` | 非 `read_only` 模式下同一连续组的独立命令默认并行，每个窗口最多 4 个进程；依赖前序结果或共享可变状态时传 `parallel=false`；`ask_before_changes` 逐个审批，但已批准的独立命令可以重叠执行 |
| 有序副作用 | `write_file`、`delete_file`、`replace_in_file`、`apply_patch`、持久终端工具、浏览器插件工具、`send_agent_message`、`wait_for_agent_message`、`set_team_collaboration_plan`、`spawn_subagent`、`wait_for_subagents`、`submit_team_work_item`、任务清单、`load_skill`、各类停止/等待工具 | 按模型返回顺序执行；审批中断、消息投递、计划发布、状态消费、团队入箱、终端/浏览器标签确认和 Skill 激活不能并发重排 |

文件变更批次在首个写入或审批前先读取并保存每个调用的 `expectedContent`。同一批对同一文件的后续调用不会以第一个写入后的内容重新生成基线，而是在应用时返回 `FILE_CHANGED` 并作废；模型必须重新 `read_file` 后生成新变更。多个不同文件可以逐个审批和写入，但当前不是多文件原子事务，部分成功后不会自动回滚。

`run_command` 的每次调用都启动一个新的无 Profile、非交互 Shell 进程；进程在命令完成、停止、超时或取消后释放，不维护可复用的 PowerShell/PWSH 进程池，也不在命令之间共享变量、当前目录或环境变更。`mode=batch` 是默认模式，适用于编译、测试、打包、迁移等所有最终会退出的命令：执行期间输出持续进入原工具项，模型必须等到真实退出后才收到最终状态，省略 `timeoutMs` 时最多运行 30 分钟。`mode=service` 只用于开发服务器、watcher 等预期长期存活的进程：在 `yieldTimeMs` 启动观察窗口内展示日志，随后返回 `commandId` 并释放项目操作锁；省略 `timeoutMs` 时不自动超时，应用退出时统一终止。服务应提供简短 `serviceName`；运行时把它与 `commandId`、启动命令、工作目录、启动时间、超时和 Shell 一起保存在活动命令注册表。`list_background_commands` 只返回当前对话仍在运行的项目，不返回 stdout/stderr；模型遗忘 ID 时先查询，再把准确 ID 交给 `wait_for_commands` 或 `stop_command`。完成、停止、超时或取消的命令立即退出活动列表，不会被下一条命令复用。该注册表属于当前桌面主进程，应用重启后不把历史 PID 重新认领为可控进程。SSH、REPL、密码输入等需要后续交互的命令不进入对话内非交互终端，使用 `terminal_control` 创建的侧边 PTY。侧边终端设置中的 Shell、启动路径和字体只控制可见 PTY；普通命令使用应用受控的默认 Shell，仅复用设置中的输出字符集做 stdout/stderr 解码。PowerShell/PWSH 启动时统一把控制台输入、输出和管道编码设为 UTF-8，避免原生程序输出的中文路径在进入 PowerShell 管道时先被旧代码页破坏。

## 3. 搜索与 ripgrep

`search_text` 和 `find_files` 底层均使用应用随包提供的 ripgrep，并遵守项目忽略文件和固定的重目录排除规则。

`search_text` 适合：

- 需要路径、行号和有界摘录的常规搜索；
- 字面量或正则搜索；
- smart/sensitive/insensitive 大小写策略；
- 少量 include/exclude glob。

`run_command` 直接执行 `rg` 适合：

- `-A/-B/-C` 上下文行；
- `-c/-l/--stats` 计数、文件名或统计输出；
- 多个 `-e` 表达式、复杂 glob 和类型过滤；
- 与排序、去重或其他命令形成管道；
- 用户需要查看完整原生命令与终端输出。

桌面命令环境会把内置 ripgrep 目录加入 `PATH`，因此不依赖用户另外安装 `rg`。两种搜索入口是互补关系，不互相禁止。`search_text`/`find_files` 的无匹配是成功空结果；`run_command` 则严格按最终退出码判断，`rg` 无匹配或被截断型管道提前关闭时可能返回非零，即使已经产生 stdout。模型应把预期失败的探测与其他检查拆开，并优先用 `maxResults` 限制结构化搜索，不通过 `Select-Object` 等消费者截断 `rg` 管道。

### 3.1 公共网页搜索

`web_search` 适用于需要当前公共网页信息、但不需要登录态或页面交互的查询。它请求 DuckDuckGo Lite，并使用 `parse5` 解析结构化 HTML，不要求 API Key 或付费搜索服务；输入支持 `query`、`maxResults`、`region` 和 `safeSearch`，请求有 20 秒超时，响应大小、结果数量、标题、摘要和 URL 均有上限。搜索失败、网络不可用或被限流时，工具返回统一错误信封，模型可以调整查询后重试，但 Runtime 不自动重放已经完成的网络请求。

该工具不读取项目文件、不要求临时对话附加工作区，也不承担浏览器导航、点击、登录、下载或页面脚本执行；这些能力仍属于后续受管浏览器工具范围。

### 3.2 临时对话的工作区边界

临时对话的定义是“没有关联项目/工作区”，而不是“不能使用工具”。没有工作区时：

- `run_command`、`wait_for_commands` 和 `stop_command` 仍可用，命令在该对话独立的系统临时目录中执行，不能访问其他临时对话的命令会话；
- `web_search`、Agent/Subagent、任务清单和已配置的附件工具仍按各自合同可用；
- `list_directory`、`read_file`、`search_text`、`find_files`、文件变更和项目冲突工具需要先附加工作区；
- `read_external_file` 不需要工作区，但读取工作区外文件前必须逐次审批；
- 命令仍受当前权限模式约束：只读模式拒绝副作用命令，询问模式逐次审批，完全访问模式只在隔离临时目录内直接执行。

附加或替换目录后，Runtime 才把文件工具和项目操作协调绑定到该目录；移除目录后，后续临时命令继续使用隔离临时目录，不回退到任意用户目录。

## 4. 生命周期合同

### 4.0 Tool Call 审计顺序

每个模型 Tool Call 以 `runId + toolCallId` 作为重放幂等键。运行时先追加 `tool_call_requested`，Handler 产生可执行提案后追加一次 `tool_execution_prepared`；需要授权且启用 AI 审批时先追加 `tool_approval_auto_reviewed`。低/中风险自动通过后直接执行且不生成长期规则；需要人工确认时再依次追加 `tool_approval_requested` 和用户的 `tool_approval_decided`。批准决定同时把 Tool 持久状态恢复为 `running` 并发出 `tool.started`，因此审批卡在副作用开始前退出等待态；完成或失败后再追加 `tool_result`。若 Run 在决定前停止、失败或重启中断，则追加 `tool_approval_expired` 并把 Tool 行终态化为 `cancelled`；该卡片不再接收审批。LangGraph 因审批恢复而重放 ToolNode 时，已完成结果会直接返回，已记录的执行意图也不会重复追加或重新执行副作用。〔FACT｜`apps/desktop/src/main/agent/agent-runtime.ts`；`apps/desktop/src/main/storage/thread-log.ts`〕

这些事件目前写入 ThreadLog 的影子审计链，并被 `EventProjector` 幂等索引；SQLite 的 Timeline/Tool 行仍是现有 UI 业务写入链。因而日志写入失败会被记录为 Main 诊断，不能据此假设 SQLite 业务事实已经可以完全由事件重建。〔FACT｜`apps/desktop/src/main/agent/agent-runtime.ts`；`apps/desktop/src/main/storage/event-projector.ts`〕

### 4.1 即时只读

```text
调用 -> 校验项目边界和参数 -> 执行 -> { ok, value }
```

结果必须有数量、行数、字节数或字符数上限。取消信号应终止仍在运行的 ripgrep 或读取操作。

### 4.2 文件变更

```text
读取当前内容 -> 准备变更和 Diff -> 等待权限审批 ->
检查文件未被外部修改 -> 临时文件写入并原子替换 -> 完成事件
```

拒绝、文件已变化、路径越界和项目操作冲突均作为当前工具失败显示。`apply_patch` 当前支持一个既有文件中的多个局部修改位置；标准 `---/+++` diff 外层若仅多出 GPT/Codex 常见的 `*** Begin Patch`、`*** End Patch` 包装行，Runtime 会先安全移除再校验，避免无意义的格式重试。若 `@@` 声明的行数与补丁正文可确定地不一致，Runtime 只根据正文重新计算行数，仍以当前文件上下文作为最终应用条件。`*** Add File:`、`*** Update File:`、`*** Delete File:` 等非标准指令不会被猜测性转换；新建和删除分别使用 `write_file`、`delete_file`，大段或整文件精确替换优先使用 `replace_in_file`。上下文不匹配时错误必须指出 `@@`/上下文问题，不能误报为缺少文件头。

编辑工具的模型提示采用与厂商无关的操作选择规则：新建使用 `write_file`，删除使用 `delete_file`，单处精确修改或整段替换默认使用 `replace_in_file`，只有同一既有文件内存在多处离散局部修改时才使用 `apply_patch`。系统提示与工具描述必须同时声明这一选择顺序；`apply_patch` 描述提供唯一标准 unified diff 示例，并明确它不是 `*** Update File:` 指令协议。格式归一化只作为后端最后一道通用容错，不能替代工具选择提示，也不得按模型名称分叉安全语义。

### 4.3 命令任务

```text
准备命令并选择 mode -> 等待权限审批 -> 启动进程
batch -> 持续输出 -> 等待真实退出 -> 返回最终状态
service -> 在 yieldTimeMs 内观察启动日志 -> 返回 serviceName + commandId
        -> list_background_commands / wait_for_commands / stop_command
```

`list_background_commands` 是找回活动 `service` 的 `commandId` 的只读入口，不替代 ID，也不按模糊服务名直接杀进程。`wait_for_commands.waitFor` 支持 `any` 和 `all`。等待超时不停止服务；服务自身配置了超时时才会终止进程树。多个独立 `batch` 命令仍可在同一轮并行执行；非 `read_only` 模式下默认并行，询问模式先逐条审批，审批通过后进入同一并行窗口，依赖命令显式传 `parallel=false`。有限耗时命令不再通过较短 `yieldTimeMs` 伪装成后台任务。

### 4.4 项目冲突

```text
文件变更发现冲突 -> 当前变更请求立即作废，返回 operationId 和占用对话 ->
必要时等待占用操作结束 -> 重新读取文件并生成新的变更
命令发现冲突 -> 当前命令请求立即作废 -> 必要时等待 -> 重新评估项目状态并生成新命令
```

协调器以规范化后的真实工作区根路径识别同一项目，不依赖 conversation/project ID；结构化文件变更再按规范化文件路径互斥，命令默认与项目写操作互斥。同一 Run 中独立命令默认进入并行窗口，`parallel=false` 才降级为串行；不同对话仍不能无感知地同时修改项目。冲突不会排队或重放已基于旧状态生成的参数；写入前还会比较 `expectedContent`，发现文件已变化时返回 `FILE_CHANGED`，要求 `read_file` 后重新生成 Diff。等待工具只用于观察占用操作状态，不能恢复被丢弃的旧请求。

### 4.5 Agent 与 Subagent

Agent 消息和 Subagent 都持久化到独立对话。任意普通对话、团队对话或临时 Subagent 都注册 `list_agent_conversations` 和 `read_agent_conversation`，可按 token 预算查询自己或任意另一条对话；临时 Subagent 不注册 `send_agent_message`、`wait_for_agent_message` 或团队计划工具。Subagent 是一次性任务，创建时不复制父对话的模型消息、压缩 Checkpoint、Timeline 或附件引用，只把 `spawn_subagent.task` 作为首条模型可见用户输入；父 Agent 必须在任务中显式提供必要背景。完成后对话只读；复杂任务的最终回答以简短完成回执开头，用单独一行 Markdown `---` 分隔支持细节，简单任务可以直接短答。Runtime 只把回执或有界短答通过隐藏的 `task_result` 自动回传给父 Agent 的模型上下文，完整结果保留在子对话且不进入父 Timeline；旧模型未输出分隔线时继续使用有界截断兜底。父 Agent 正常情况下也不为取回结果主动列出或读取子对话，只在结果明确截断且影响任务，或用户要求审计时按预算读取。Team Lead 与团队成员不使用 Subagent：用户先从模板显式创建 global、project 或 conversation TeamInstance，实例内再复用持久 Team Lead/成员 Conversation；模型交办只选择当前来源可见的 `teamInstanceId`，不能隐式创建另一支团队。Lead 只通过 `send_agent_message` 和 `wait_for_agent_message` 分派、等待和汇总。`send_agent_message(expectReply=true)` 可用 `replyInstruction` 指定完成回执重点；成员以单独一行 Markdown `---` 分隔回执和详细结果，完整最终回答保留在成员 Conversation，Runtime 只向发送方投递分隔线前最多 1000 字符和来源 Conversation ID。`list_agent_conversations` 返回显式运行状态；`read_agent_conversation(maxTokens, query?, beforeSequence?, historyScope?)` 默认查询最新 Checkpoint 已覆盖的原始历史，没有 Checkpoint 时回退到全部历史，显式传 `historyScope=all` 才跨越 Checkpoint 查询整条历史。关键词最少 1 个字符，命中后带回所属完整原始轮次；`pagination.nextBeforeSequence` 用于继续读取更早页，全部结果仍受 Token 预算约束。团队成员消息只能投递给同一持久 TeamInstance 的参与者，且每条任务分派绑定 WorkItem 记录以供审计。

来源主 Agent 交办 WorkItem 后可以继续本对话的其他工作，不自动等待团队，也不把协作投影持续注入每轮请求。`get_team_work_item_status` 允许它稍后按需恢复近期 WorkItem ID，或读取一个 WorkItem 的有界任务计数、参与者运行态、最近输出和结果摘要；查询按来源 Conversation 授权，结果不含完整需求、事件、协作边、节点坐标或成员完整 Timeline。

受管 WorkItem 的 Team Lead 还可调用 `set_team_collaboration_plan`。它先用 `list_agent_conversations` 取得当前参与者 Conversation ID，再在第一次委派前发布完整有向路线；后续变化发布下一版完整修订。模型不能传入 `workItemId`，Main 从调用者所属的运行中 WorkItem 补齐，并要求调用者正是根 Team Lead。计划是软约束：发布失败返回真实工具错误，但没有计划边仍可合法调用 `send_agent_message`，实际偏差会被统一投影标记为 `ad_hoc`。投影只统计方向、类型、已读状态和时间，不把消息正文复制到图中。〔FACT｜`apps/desktop/src/main/agent/agent-communication-tool.ts`；`apps/desktop/src/main/storage/agent-database.ts`〕

`spawn_subagent.icon` 的模型工具 Schema 继续声明受控枚举，Renderer 只映射本地 Lucide 组件，不接收模型提供的任意 SVG、图片 URL 或组件名。执行端将枚举外的可选值归一化为“未提供”，避免装饰参数阻断 Subagent 创建。合法显式图标或所选 Agent 的预设图标仍随子 Conversation 持久化；没有可用图标时保持 `avatarIcon = null`，Renderer 根据子 Conversation ID 即时生成稳定的 5×5 对称 Identicon。`name` 只负责身份展示，`task` 继续作为实际模型输入且不参与头像哈希。

等待超时只表示当前 Agent 暂不继续阻塞。Subagent 或目标对话完成后仍可通过运行时事件唤醒来源对话。

### 4.6 任务清单

```text
create -> update（最多一个 running）-> close
```

任务清单只适用于确实需要多步推进的任务。简单任务不应为了调用工具而创建清单。状态发生实质变化时才更新，并把同一批变化合并到一次完整更新中；能与实际读取、检查或收尾工具同轮调用时，不为清单元数据单独消耗模型轮次。完成后模型可以主动关闭，用户也可以手动关闭。`blocked` 和 `failed` 步骤必须通过 `reason` 提供一条简短原因；其他状态不携带该字段，详细诊断仍归属 Assistant 或工具 Timeline。

### 4.7 Skill 上下文

```text
系统上下文注入 Skill 目录摘要
  -> 模型调用 load_skill(skillId)
  -> Runtime 校验作用域、启用状态、MCP 依赖并读取 SKILL.md
  -> Graph State 保存 id/version/contentHash
  -> 下一轮模型请求临时注入正文
  -> 按需调用 read_skill_reference(skillId, relativePath)
```

Skill 正文是本轮模型输入，不是聊天历史。每个 Skill 必须位于与 Frontmatter `name` 同名的父目录中；Runtime 首次登记时由该名称生成稳定 `skillId`，全局重名使用数字后缀区分，模型只能从当前目录摘要选择 ID，不能猜测文件路径。ContextManager 在历史裁剪前固定预留正文预算，正文实际注入和用量展示使用同一预算；无模型窗口时按 48,000 Token 计算。工具结果只返回加载摘要和哈希；`read_skill_reference` 只允许已激活 Skill 的 `references/`、`templates/` 相对 UTF-8 文本文件，读取前检查字节上限，并拒绝绝对路径、遍历、符号链接、NUL、非法 UTF-8 和超大内容。`custom` Agent 的 `skillIds` 必须统一约束目录、加载、reference 与恢复注入，不能只隐藏目录；`inherit_all` 才继承全部满足作用域的 Skill。`project`/`team` Skill 分别要求项目工作区/团队会话。Skill 内容不能覆盖系统安全规则、项目授权范围或审批策略。Run 结束后 Graph thread 会清理；会话删除任务也会清理对应 threads。

### 4.6 权限授权范围

审批卡提供“允许一次”“本对话允许相同操作”和“拒绝”。会话授权由 Runtime 按 conversation 管理，只对完整相同命令或同一工具与相同路径生效；卡片不提供模糊命令族或 Agent 永久授权。Agent Profile 中仍可由用户在设置页显式维护 allow 规则，末尾 `*` 前缀规则不由一次审批自动生成。持久终端创建和命令使用同一 `run_command` 权限类型；浏览器可见副作用使用独立 `browser_control` 权限类型。选择 AI 帮我审批时，仅低/中风险明确操作自动通过，删除、工作区外读取、破坏性/提权命令和任何不确定情况仍请求用户审批。

### 4.7 工作区终端标签

```text
模型调用 terminal_control(action=create, name?) -> Runtime 固定当前 conversation/project ->
准备待批准动作 -> 权限判定/必要时 interrupt 审批 ->
批准后进入项目命令操作协调并创建 PTY -> Main 请求 Renderer 创建标签 ->
Renderer 计算唯一名称并确认 ->
工具结果 { requestedName, resolvedName, nameAdjusted }
```

这条工具链只创建项目右侧的用户可见标签，不写文件、不执行模型传入的 Shell 命令，也不接受模型指定的绝对路径、Shell 或环境变量。普通命令、构建、检查和测试默认走 `run_command`，不会打开可见标签；只有用户明确要求可见/右侧/交互终端，或任务确实需要用户可观察、可接管的持续 PTY 时，模型才调用本工具链。批准前不得创建 PTY 或请求 Renderer 建标签；LangGraph 恢复时复用同一待批准动作，以 `runId + toolCallId` 防止重复创建。标签名称必须是 1 至 120 个字符；若请求名与任一已打开标签重名，Renderer 依次使用 `名称 (1)`、`名称 (2)`，并以确认结果作为模型可见的唯一事实。确认超时或当前会话不再对应请求项目时，工具失败，模型不得假定终端已打开。

`terminal_control action=write` 同样先审批，再在项目命令操作协调边界中向所属 PTY 写入一次并读取有界即时输出；`action=list/read` 只读当前对话拥有的活动终端和有界转录，不审批；`action=close` 先审批，再关闭 PTY 并通知 Renderer 移除对应标签。`yieldTimeMs` 只是即时采样提示，通常省略；超过 5000 毫秒时按 5000 毫秒执行，不因模型给出过大值让整次调用失败。协调记录在发送和即时采样结束后释放，长时间运行的开发服务器继续由 PTY 会话生命周期管理，不能用一个永久项目写锁阻塞用户后续编辑。

外层 PTY 存活与内层 SSH 连接是两个状态。可复用 SSH 必须以不带远程命令的交互式 `ssh [options] user@host` 建立，认证完成且 `terminalContext.kind=ssh_connected` 后，后续服务器命令都传 `expectedContext=ssh`。工具增量检查终端输出；若出现连接关闭、重置、拒绝、超时、域名解析失败、Host Key 校验失败或认证失败，则标记 `ssh_disconnected` 并以 `TERMINAL_CONTEXT_MISMATCH/reconnect_ssh` 拒绝远程投递，避免命令误落到已经恢复的本地 Shell。写入前发现断线时结果明确为 `delivery=not_sent`，重连后可以重试；写入后才发现断线时结果为 `delivery=unknown`，模型必须先核对远端实际状态，不能自动重放可能已有副作用的命令。`ssh host command` 是一次性远程执行，结束后仍属于本地上下文，不能当作持久连接复用。

PTY 与 `terminalId` 只在当前桌面主进程和终端进程存活期间有效，不能从历史消息恢复已经退出或因应用重启丢失的 PTY。用户从界面手动打开的终端不会自动归属当前模型对话，模型不得猜测或复用其 ID；`action=list` 只返回当前应用会话中由 `terminal_control` 为当前对话创建且仍存活的终端。执行或读取遇到失效 ID 时返回稳定的 `TERMINAL_UNAVAILABLE` 和 `recreate_terminal` 恢复指令，模型先 `list` 查找活动终端；没有可用项时再 `create`，不得重复重试旧 ID。

### 4.8 首方浏览器动作

`browser_control` 通过 `action` 区分打开、列出当前对话会话、观察、截图、导航/前进/后退/刷新/停止、点击、移动、按下/释放、拖拽、填写、选择、按键、滚动、等待和关闭。打开、导航与所有可改变页面状态的动作先生成待批准动作，使用同名权限类型；批准前只能解析参数并校验目标会话仍归属当前项目对话。填写/选择内容仍保留在当前 Tool Call 参数和审计记录中，但持久允许规则只保存动作及元素引用，不保存输入正文。`action=list`、`action=observe`、`action=screenshot` 和 `action=wait` 为只读动作，可直接执行。语义操作使用最新元素引用；视觉操作使用最新截图的 CSS 像素坐标并通过 Chromium 原生输入分发执行。截图通过内部模型附件返回，工具审计结果仅保存尺寸和坐标系等元数据；模型请求只携带最新浏览器截图字节，避免连续视觉观察撑大上下文。取消信号在批准后执行副作用前再次检查；审批拒绝或应用策略 `unavailable` 时不调用受管浏览器控制器。复杂浏览器任务应先加载 `browser-use` Skill；静态公共资料优先 `web_search`，稳定的非交互网络或项目操作优先 `run_command`。

Subagent 只继承创建时的权限模式，父对话的 `once`/`session` 规则不会跨 conversation 复制；显式配置的 Agent allow 规则仍可按绑定 Agent 共享。审批事实始终属于子 Tool/Run，子页面保留完整卡片，父页面只从活动子 Run 的 `awaiting_approval` Tool 派生一个输入框上方的操作入口。两个入口提交同一 `runId + toolId`，先处理后再次提交返回 `APPROVAL_EXPIRED`。

## 5. 统一结果与展示

### 5.1 模型可见工具合同

模型请求只发送工具名、用途描述和输入 JSON Schema。输入 Schema 由执行端的 Zod Schema 使用 `io=input` 自动生成，并移除 provider 无需的 `$schema` 元字段；因此默认值、必填字段、枚举、UUID、长度上限和对象的 `additionalProperties` 与实际执行校验保持同一来源。无法用 JSON Schema 表达的跨字段规则、规范化和安全校验（例如 `read_file` 的 `startLine/endLine` 范围、任务清单最多一个 `running` 步骤、字符串 `trim` 和工作区路径边界）继续由执行端 Zod 或领域校验，并在工具描述或错误结果中说明；能定位到具体字段的规则应通过字段级 issue 返回。Provider 返回的参数先由统一解析器处理；非法 JSON 或非对象参数使用稳定的 `TOOL_ARGUMENTS_INVALID` 错误码和字段问题，不依赖错误文案分类。

工具校验失败时，除现有 `agentError`、`error` 和 `ok=false` 外，模型可获得有界的：

```json
{
  "recovery": {
    "action": "fix_arguments",
    "instruction": "根据 issues 修正本次工具参数后重试；不要重复提交相同参数。",
    "issues": [
      { "code": "invalid_format", "path": ["commandId"], "message": "Invalid UUID" }
    ],
    "retryable": true
  }
}
```

`issues` 最多保留 8 项，字段路径、校验代码和消息均有长度上限。`recovery.retryable=true` 只表示模型可以根据问题修正参数后发起一个新请求；它不等于运行时自动重放，文件、命令和审批等有副作用操作仍必须遵守其原有权限和生命周期。文件版本过期或文件变更发生项目操作冲突时使用 `recovery.action=reread_and_rebuild_change`，明确要求丢弃旧参数、重新读取当前文件并生成新的变更；命令冲突只返回等待和重新评估指引，不伪装成文件恢复。

内置工具成功结果统一使用：

```json
{ "ok": true, "value": {} }
```

参数校验和非预期执行异常使用统一 AgentError 信封；审批拒绝、命令非零退出和项目操作冲突等预期生命周期结果使用 `{ ok: false }`，并在 `value` 中保留退出码、标准输出、标准错误、超时、截断或占用者信息。经过 LangGraph `ToolNode` 的工具适配器如果抛出非取消、非审批 `interrupt` 异常，Runtime 必须转换为有界的失败 `ToolMessage`，将工具行持久化为 `status=failed` 并发送一次 `tool.completed`；不得把诊断异常吞掉，也不得自动重放该副作用调用。取消和审批中断必须传播给图执行器；如果上游编译图把顶层 `GraphInterrupt` 作为异常抛出，执行边界必须把它归一为同一审批暂停/恢复流程，不能将 Run 标记为内部错误。Subagent 已改为相同成功信封，避免专用 UI 无法解析。

UI 展示规则：

- 命令展示 Shell、完整命令、stdout/stderr、退出状态和 `commandId`。
- 文件变更展示红色删除、绿色新增的 Diff；错误留在同一变更卡内。
- 目录、搜索、附件和跨对话读取使用有界结构化结果。
- 项目占用展示操作内容、占用对话和状态。
- 多工具调用按批次折叠，用户按需展开，不默认铺开所有原始 JSON。

## 6. 当前缺口与扩展顺序

### 6.1 下一阶段必须补齐

1. MCP Runtime：当前已有配置、编辑和存储，但桌面能力标记仍为 `mcp: false`，还没有动态发现和执行 MCP 工具。
2. Skill Runtime：已接入 `AgentRuntime + LangGraph`。目录摘要、`load_skill`、正文哈希快照、统一预算的正文临时注入和受控 reference 读取已有自动测试；运行中的 Skill 文件发生变化会拒绝静默恢复。仍未实现 Skill 脚本执行、真正的 MCP Runtime 依赖和 running Run 的自动重放。
3. 多文件补丁事务：一次审批多个文件时，需要批量 Diff、逐文件并发冲突检查、失败回滚和多文件刷新事件，不能直接复用单文件 `PreparedFileChange`。

### 6.2 按需求再增加

- 浏览器文件传输与凭据：当前浏览器插件已提供会话恢复、语义观察、模型可见截图、前进/后退/刷新/停止、真实鼠标点击/移动/按下/释放/拖拽、文字输入、表单填写/选择、键盘、滚动、条件等待和关闭。文件上传、下载产物读取、保存密码/自动填充与外部浏览器登录态接管仍需另行设计边界；不能把用户浏览器的 `WebContentsView` 直接暴露为任意脚本执行环境。
- 持久 PTY 的回收与完成检测：当前模型可读写自己在项目对话中创建的侧边 PTY，创建和命令均已接入统一审批与项目操作协调；普通构建和测试仍优先使用有界、可等待和可取消的后台命令任务。任意交互 Shell 无可靠的跨 Shell 命令完成边界，因此项目操作协调只覆盖命令发送和即时输出采样，不覆盖后台进程的全部存活期；跨对话共享、任意 Shell/工作目录参数及临时对话 PTY 仍未开放。
- Git 专用模型结果：右侧用户 Git 工作区已经提供结构化状态、文件级 Diff 与受限的人工 Git 操作；这些 IPC 只服务用户界面，不成为模型工具。模型仍使用 `run_command` 执行 `git status/diff/log`，需要提交图或可点击 hunk 时再设计独立工具合同。
- 二进制文件写入、移动和复制：明确产品场景后设计审批和预览，当前不通过文本工具伪装支持。

### 6.3 当前不引入

- 不增加 `get_current_directory` 或 `get_project_info`。项目对话已经绑定动态工作目录。
- 不增加 `run_rg`、`run_git`、`run_test` 等 Shell 薄包装。
- 不增加万能 `wait`。命令、Agent 消息、Subagent 和项目冲突的完成条件及返回值不同。
- 不引入向量检索工具替代代码搜索。文件名、文本和符号定位优先使用 ripgrep；达到明确语义检索需求后再评估索引。

### 6.4 外部开源实现的复用边界

- 不直接搬运 PI Agent / Codex 的完整工具运行时：它们分别绑定自己的 TypeScript Harness 与 Rust CLI / Sandbox 生命周期，直接复用会绕开本项目既有的审批、项目锁、Timeline 和 IPC 合同。
- 优先复用已经处于稳定边界的底层库，并保留本项目的适配层：例如 `@vscode/ripgrep` 用于受限搜索、`diff` 用于变更展示、`file-type` / `mammoth` / `unpdf` 用于文件识别与提取。库升级或替换只影响对应 Handler，不改变 `ToolRuntime` 的统一权限和结果合同。
- 新引入第三方工具前先补“Schema、权限、幂等、结果上限、取消、错误信封、测试矩阵”；没有通过该矩阵，不因为开源项目热门而接入生产主链。

## 7. 测试门禁

每次调整工具至少验证：

1. 参数 Schema 与模型看到的 JSON Schema 一致。
2. 项目路径不能越界，忽略规则和结果上限生效。
3. 成功和失败信封能被 Agent Runtime 与 UI 同时解析。
4. 审批拒绝、文件外部变化、命令非零退出和超时显示在原工具卡中。
5. 后台命令可以等待、停止；并行命令不会串输出或复用错误 ID。
6. 两个对话操作同一项目时能返回占用者，并在等待后重试。
7. Subagent 和 Agent 消息等待超时不会取消后台任务或丢失后续唤醒。
8. Skill 的正文不进入 Timeline；reference 只能读取允许目录的有界文本，文件变化和越界路径返回可恢复错误。
